# 题库答案 · 与出处原文比对报告

核对范围：936 张带出处链接的卡（共 24/24 批完成）

报出问题：**150** 条 —— low 83、medium 64、high 3

> 这份报告只做判定，不动卡片。原文正文没有、也不会进入任何卡片。


## 按类型

| 类型 | 条数 |
|---|---:|
| 我们写错了 | 57 |
| 原文有问题（我们对） | 43 |
| 漏了必答点 | 43 |
| 与原文冲突 | 7 |

## 按牌组

| 牌组文件 | 条数 |
|---|---:|
| `10-golang.json` | 23 |
| `02-test-design-process.json` | 15 |
| `15-computer-network.json` | 14 |
| `06e-langchain.json` | 13 |
| `06a-llm-engineering.json` | 11 |
| `03c-web-automation.json` | 9 |
| `04-performance-observability.json` | 8 |
| `06c-agent.json` | 7 |
| `06d-tool-calling-mcp.json` | 7 |
| `03b-api-automation.json` | 7 |
| `06b-rag.json` | 6 |
| `20c-message-queue.json` | 6 |
| `03d-app-automation.json` | 5 |
| `16-database.json` | 4 |
| `17-redis-cache.json` | 4 |
| `11-data-structures-algorithms.json` | 3 |
| `14-operating-system.json` | 2 |
| `18-linux-troubleshooting.json` | 2 |
| `20b-distributed.json` | 2 |
| `20a-system-design.json` | 1 |
| `19a-git.json` | 1 |

---

## 严重（记进脑子会答错） · 3 条

### `06c-agent#13` · 与原文冲突 · 把握 high

**题**：Agent 推理模式有哪些？ReAct 是啥？具体是怎么实现的？

- 我们：Thought 给人看的成分大于给模型看，有效的是 Observation
- 原文：Act-only 的准确率明显低于 CoT 和 ReAct
- 问题：ReAct 原论文的消融就是「去掉 Thought 只留 Action」后效果显著下降（HotpotQA、ALFWorld 均如此），说明 Thought 首先是在提升模型自己的决策质量，不是给人看的装饰品；这句加分点反而会让人在被追问「为什么要显式写 Thought」时答错
- 建议：改成：Thought 既提升模型决策质量（拿掉它的 Act-only 版本效果明显更差），也顺带方便人排障；而 ReAct 相对纯 CoT 的增量才是来自 Observation 引入的外部事实

### `06e-langchain#7` · 与原文冲突 · 把握 high

**题**：请你详细说说 LangChain 和 LangGraph 的核心区别是什么？

- 我们：对比表：LangChain 持久化「无」、人在环「难表达」、循环要在框架外自己写 while
- 原文：现在 LangChain Agent 就运行在 LangGraph 上
- 问题：我们把 LangChain 等同于旧式线性链来对比，断言它没有持久化、难做人工介入、循环要自己套 while；实际 v1 的 create_agent 返回的就是编译后的 LangGraph 图，检查点、流式、人工审批中间件都能直接用，这几格是错的
- 建议：把对比轴从「链 vs 图」改成「高层 Agent 框架 vs 低层编排运行时」，明确持久化/流式/人在环由同一个底层运行时提供，两者差的是控制粒度而不是有没有

### `06e-langchain#9` · 漏了必答点 · 把握 high

**题**：LangGraph 相比于 LangChain 有哪些核心优势？更适配哪些 Agent 场景？

- 我们：长任务崩溃后从断点继续，不用重跑（重跑一次可能几十块钱）
- 原文：节点恢复时会从节点开头重新执行
- 问题：只讲了检查点能恢复，没讲恢复粒度是节点级、被中断的节点会从头再跑一遍，也没讲外部副作用必须自己保证幂等；照这个理解写代码会出现重复扣款、重复发信
- 建议：在检查点那条后面补上恢复语义与幂等要求：带业务幂等键、先查后写、把有副作用的动作单独划成可记录的任务边界


---

## 中等（不准确但不致命） · 64 条

### `02-test-design-process#109` · 我们写错了 · 把握 high

**题**：4. H5页面性能测试关注哪些核心指标？

- 我们：表格里交互指标写的是「首个输入延迟」（FID），全文没出现 INP
- 原文：FID (First Input Delay)：首次输入延迟
- 问题：交互响应指标已由 INP 取代 FID（2024 年 3 月 INP 转正、9 月 FID 退役），卡片和原文都停在 FID，2026 年面试按这个答会被判为过时
- 建议：把交互指标改成 INP 为主、FID 标注为已废弃的旧指标，顺带说明 INP 衡量的是整页所有交互的响应延迟而非只看首次

### `02-test-design-process#174` · 原文有问题（我们对） · 把握 high

**题**：30. 如何设计用例来测试接口的幂等性？

- 我们：DELETE 首次 204、二次 404，仍然是幂等的
- 原文：后续请求都应返回与第一次相同的成功状态
- 问题：原文把幂等定义成"多次请求返回相同状态码"，这是常见误解——幂等约束的是服务端状态，返回码可以不同。我们的写法是对的，这条是原文的问题。
- 建议：我们不用改；只需注意别有人照原文回改，可在卡里再点明一句"返回码相同不是幂等的判定标准"。

### `02-test-design-process#46` · 我们写错了 · 把握 high

**题**：19. 你如何评估你的测试是否足够充分（Test Coverage）?

- 我们：语句 < 分支 < 条件 < 条件组合/路径 < MC/DC，层级越往上越严成本越高
- 原文：原文只提代码覆盖率工具，未给层级
- 问题：把 MC/DC 排在条件组合覆盖之上是反的：MC/DC 恰恰是为了避开条件组合覆盖 2^n 用例爆炸而设计的更弱、更省的准则，条件组合覆盖包含 MC/DC；把它说成最严最贵会答错追问
- 建议：改成条件组合覆盖最强最贵、MC/DC 位于判定条件覆盖与条件组合覆盖之间，并说明它是高安全领域为控成本选的折中准则

### `03c-web-automation#17` · 原文有问题（我们对） · 把握 medium

**题**：15. 如何处理富文本编辑器？

- 我们：直接 innerHTML 赋值往往「看着写进去了，保存后是空的」
- 原文：用 JS 执行 arguments[0].innerHTML 直接设置内容
- 问题：原文把 JS 赋 innerHTML 当首选做法，但现代编辑器与前端框架监听的是输入事件，直接改 DOM 内部数据模型不同步，保存时会丢内容
- 建议：保持我们的写法（补派发 input 事件或调编辑器 API），可点名原文这条是典型踩坑做法

### `03c-web-automation#19` · 原文有问题（我们对） · 把握 high

**题**：17. 失败重试机制怎么实现？

- 我们：用 IAnnotationTransformer 全局挂 RetryAnalyzer
- 原文：testng.xml 里 <listener> 配 RetryAnalyzer 全局生效
- 问题：IRetryAnalyzer 不是 TestNG 监听器，注册成 listener 不会生效；要全局化只能靠 IAnnotationTransformer（或套件级 retryAnalyzer 配置），我们的写法才是对的
- 建议：不用改我们的答案，可加一句提醒「把 RetryAnalyzer 当 listener 注册」是网上流传的错误写法

### `03c-web-automation#23` · 我们写错了 · 把握 medium

**题**：4. 如何定位App元素？常用哪些定位方式？

- 我们：React Native：testID（会映射到两端的无障碍标识）
- 原文：（此点原文未涉及）
- 问题：RN 的 testID 只有 iOS 侧是 accessibility identifier，Android 侧暴露成 resource-id 而不是 content-desc，按这句写会在 Android 上用错定位方式
- 建议：改成「iOS 映射为 accessibility identifier、Android 映射为 resource-id，两端分别用 ACCESSIBILITY_ID 和 ID」

### `03c-web-automation#32` · 原文有问题（我们对） · 把握 high

**题**：6. Selenium中的等待机制有哪些？如何选择？

- 我们：要么全局关掉隐式等待只用显式，不要混用
- 原文：项目初期设置一个较小的隐式等待，比如5秒兜底
- 问题：原文建议隐式等待兜底再叠显式等待，而官方明确警告二者混用会让实际等待时间不可预测；我们的说法是对的，原文这条是反模式
- 建议：不用改我们的答案，可以顺手在卡里点名「设个小隐式等待兜底」是常见错误建议

### `03c-web-automation#41` · 我们写错了 · 把握 high

**题**：15. 如何实现失败截图和录屏？

- 我们：取不到 page 就用 driver，之后仍调 screenshot() 和 content()
- 原文：（此点原文未涉及）
- 问题：hook 里回退到 Selenium 的 driver 之后，仍然调用 Playwright 专属的 page.screenshot() 与 page.content()，Selenium 侧没有这两个方法，照抄会 AttributeError
- 建议：把 Playwright 分支和 Selenium 分支拆开写，Selenium 侧改用 get_screenshot_as_png() 和 page_source

### `03d-app-automation#22` · 我们写错了 · 把握 medium

**题**：8. 如何实现滑动操作（上下左右滑动）？

- 我们：滑动时长决定手感；代码把 duration_ms 传给 pause()
- 原文：原文用 driver.swipe(..., duration)
- 问题：示例里 duration_ms 只是按下后的停顿时间，真正的移动时长走的是 move_to_location 的默认值（250ms），所以调这个参数并不能改变滑动快慢和惯性，和正文"时长决定甩/拖"的说法对不上（Java 那张卡是正确的写法）。
- 建议：把时长传进移动动作，比如 move_to_location(ex, ey, duration=duration_ms) 或在 ActionBuilder 上设 duration，pause 只留一个很短的按压时间。

### `03d-app-automation#26` · 我们写错了 · 把握 high

**题**：15. 如何实现Android和iOS双平台兼容？

- 我们：self.LOC[name][platform.capitalize()]
- 原文：原文用 if self.platform == 'Android' 判断
- 问题：Python 里 "iOS".capitalize() 得到 "Ios"，而 LOC 字典的键是 "iOS"，所以 iOS 分支必然 KeyError；同一段示例里 login() 用到的 pwd_input 也没在 LOC 里定义。
- 建议：去掉 capitalize()，直接用 capabilities 里的 platformName 原值当键（或统一转小写后比较），并把 pwd_input 的定位器补进 LOC。

### `03d-app-automation#28` · 原文有问题（我们对） · 把握 medium

**题**：17. 如何进行弱网络测试？

- 我们：tc 跑在网关/代理机上注入延迟与丢包
- 原文：adb shell tc qdisc add dev wlan0
- 问题：原文让在手机里直接跑 tc，普通未 root 的安卓设备既通常没有这个命令、也没有改网络队列的权限，这条基本执行不了。我们把 tc 放在代理机/网关上才是可行的做法。
- 建议：保持现状；可选补一句"tc 要跑在代理机或网关上，直接 adb shell 跑一般没权限"，避免有人照原文抄。

### `04-performance-observability#38` · 原文有问题（我们对） · 把握 high

**题**：9. JMeter如何模拟弱网环境？

- 我们：JMeter 本身不是弱网模拟工具，正经做法是网络层注入延迟丢包（tc netem）
- 原文：设置为600，表示每分钟最多60个请求，相当于每秒1个
- 问题：原文错两处：一是把限制吞吐量当成模拟弱网（定时器只能拉长请求间隔，造不出链路延迟/丢包/抖动）；二是单位算错，Constant Throughput Timer 填 600 是每分钟 600 次即每秒 10 次，原文自己在第15题里也承认 100 TPS 要填 6000
- 建议：我们这张卡不用改，保持「JMeter 只负责发压、弱网靠 tc/Clumsy/Charles 注入」的写法；可在卡里顺手点明「用吞吐量定时器冒充弱网是常见错误答案」

### `04-performance-observability#41` · 我们写错了 · 把握 high

**题**：12. JMeter的聚合报告中各项指标含义是什么？

- 我们：Average 明显小于 Median……说明分布很不均匀，去查 GC、慢查询、锁竞争
- 原文：（原文未涉及该判据）
- 问题：方向写反了：GC/慢查询/锁竞争造成的右偏长尾会让平均值高于中位数，而不是低于；同一张卡自己举的例子也是 Average 60ms > Median 10ms，前后矛盾
- 建议：把这条判据改成「Average 明显大于 Median」时才怀疑长尾，理由与卡内 990×10ms + 10×5000ms 的例子保持一致

### `06a-llm-engineering#18` · 漏了必答点 · 把握 medium

**题**：大模型部署有哪些主流方案？vLLM、TGI、llama.cpp、SGLang 实际项目里怎么选？

- 我们：vLLM 主打连续批处理与分页式 KV 显存管理；SGLang 前缀复用做得强
- 原文：RadixAttention 是 SGLang 的核心创新
- 问题：机制描述到位，但全卡没出现 PagedAttention / RadixAttention 这两个名字，而这题的第一层追问就是「PagedAttention 是什么」「RadixAttention 和它什么关系」。
- 建议：在 vLLM、SGLang 两行各补一句术语名 + 一句机制（分页式 KV = PagedAttention，前缀树共享 = RadixAttention），保留原有「以自己压测为准」的立场。

### `06a-llm-engineering#20` · 漏了必答点 · 把握 medium

**题**：大模型能力评测指标有哪些？

- 我们：① 通用能力基准：知识、推理、数学、代码、多语言、长上下文
- 原文：MMLU 测综合知识，HumanEval 测代码
- 问题：整张卡没有出现任何一个具体 benchmark 名字，只有抽象维度；这题面试的第一层就是要能报出名字并说清每个测什么。
- 建议：在「通用能力基准」那一层补一个三四行的小表：MMLU/MMLU-Pro 测知识广度、HumanEval/MBPP/SWE-bench 测代码、GSM8K/MATH/GPQA 测数学与科学推理、MT-Bench/Arena 测对话偏好。

### `06a-llm-engineering#21` · 漏了必答点 · 把握 high

**题**：大模型微调的方案有哪些？

- 我们：谱系表把 LoRA/QLoRA、全参数 SFT、偏好对齐(DPO/RLHF) 并列成一列
- 原文：改哪些参数和学什么目标是两个正交维度
- 问题：把「改哪些参数」（全参/LoRA/QLoRA）和「学什么目标」（SFT/DPO）压进同一张并列表，读起来像要在它们之间二选一，实际是可以任意组合的两条正交轴。
- 建议：在谱系表前加一句或把表拆成两栏，点明这是两条轴，并给出 QLoRA+SFT、LoRA+DPO 这类常见组合。

### `06a-llm-engineering#26` · 我们写错了 · 把握 high

**题**：请讲一下 LoRA 技术，除了减少参数量，它还有哪些优点？

- 我们：A 是 d×r 降维、B 是 r×d 升维，h = Wx + (α/r)·BAx
- 原文：原文同样写 A 为 d×r、B 为 r×d
- 问题：两个矩阵的形状写反了：按我们自己给的列向量公式 h=Wx+BAx，降维的 A 应是 r×d、升维的 B 应是 d×r，否则 BA 乘出来是 r×r 而不是 d×d，公式根本不成立。
- 建议：把 A 改成 r×d（先把 d 维压到 r 维）、B 改成 d×r（再升回 d 维，仍初始化为 0），保证 ΔW=BA 是 d×d。

### `06a-llm-engineering#28` · 漏了必答点 · 把握 medium

**题**：对比使用过哪些主流大模型？你们项目中最终选用了哪个模型？为什么？

- 我们：避免给具体的榜单分数、参数量、上下文长度这类数字；全文一个模型名都没出现
- 原文：讲出每家模型的「特长」而不只是「排名」
- 问题：题目前半句问的就是「对比过哪些主流大模型」，我们只给了选型流程与判据模板，没有任何模型阵营或能力特长的落点，照着这张卡答会像在回避问题
- 建议：在流程之外补一小段阵营速览：分「国内可落地候选」「海外能力标杆」两类，各写一句能力特长与现实约束（合规出境、访问稳定性、成本），仍然不写榜单分数和版本号

### `06a-llm-engineering#31` · 漏了必答点 · 把握 medium

**题**：SFT 之后还有哪些 Post-Training？RLHF、DPO、GRPO、拒绝采样什么关系？

- 我们：拒绝采样是最简单的版本，RLHF 是完整版，DPO 是省掉强化学习的版本
- 原文：这五类方法不是替代，真实对齐流程通常组合使用
- 问题：我们把四种方法讲成互相替代的「版本」，mermaid 也是从 SFT 平行分叉；实际主流 pipeline 是串起来多轮交替（拒绝采样热身 → RL → 用 RL 后的模型采样筛数据回灌 SFT → 再 RL），读者容易误以为要四选一
- 建议：在流程图末尾或关系表后加一句，点明生产对齐流程是这几种方法串行组合、多轮交替，不是单选题

### `06a-llm-engineering#33` · 原文有问题（我们对） · 把握 high

**题**：大模型量化是什么？INT8/INT4/AWQ/GPTQ 怎么选？

- 我们：量化前面的权重时把误差摊到后面还没量化的权重上
- 原文：把误差补偿到下一层去
- 问题：GPTQ 的误差补偿发生在同一层内部、摊给该层尚未量化的权重（OBS/Hessian 那套），不是跨层补到下一层。原文简答段写成「补偿到下一层」是错的，它自己的详解段又写成「反向修正后面还没量化的权重」，前后自相矛盾；我们这版是对的
- 建议：不改，保持现有写法；如果以后有人拿原文来「订正」这张卡，以同层内补偿这版为准

### `06a-llm-engineering#35` · 漏了必答点 · 把握 high

**题**：大模型的参数：温度值、Top-P、Top-K 分别是什么？各个场景下的最佳设置…

- 我们：场景建议（用相对描述，不给死数字）；温度尽可能低 / 低到中 / 中高温
- 原文：精确任务 0~0.2，日常 0.5~0.7，创意 0.8~1.2
- 问题：题目直接问「各个场景下的最佳设置」，我们整张卡没有任何可落地的取值区间，全是「低温/中高温」这类相对词；温度区间不像模型名那样会过期，缺了它这题等于没答后半句
- 建议：在场景表里加一列参考区间（抽取/函数调用接近 0，问答与摘要偏低，创意中高，top_p 保持默认或略收），并注明这是起步值、要按自己的评测集微调

### `06a-llm-engineering#38` · 漏了必答点 · 把握 high

**题**：什么是大语言模型？和传统 NLP 模型有什么区别？

- 我们：新出现的能力：指令跟随、上下文学习、组合与工具使用、跨任务迁移
- 原文：涌现能力……规模到某个临界点之后突然表现出来
- 问题：原文把「涌现能力」列为这题最关键的加分点、也是 LLM 区别于传统 NLP 的最核心特征，我们整张卡没有出现「涌现」这个词，也没讲「规模跨过临界点后冒出没被显式教过的能力」这层因果。我们那节「新出现的能力」列的正是涌现的例子，却没把机制和名字点出来，答题时会缺一个最容易拿分的收尾。
- 建议：在「新出现的能力」前加一句定调：这些能力叫涌现，是规模跨过某个量级后冒出来的、训练目标里没显式教过的能力；再补半句业界对「是否只是评测指标离散造成的假象」仍有争议。顺便把对比表的「输出形式」那行点明是「判别式 vs 生成式」的范式差别。

### `06b-rag#12` · 漏了必答点 · 把握 high

**题**：了解哪些更复杂的 RAG 范式？

- 我们：自反思型（Self-RAG 一类思路）…代价是调用次数增加
- 原文：需在专门构造的数据集上对基础 LLM 做监督微调
- 问题：Self-RAG 的四个 reflection token 是靠监督微调训进词表的，不能拿通用模型直接 prompt 出来；我们只把它写成「让模型多做几次判断」，并把代价说成调用次数增加，落地门槛（要训模型）这一条完全没有
- 建议：在自反思型那段补一句：原论文的 Self-RAG 需要微调基础模型把反思标记训进去，通用模型只能用 prompt 近似；真正的代价是多一条训练流水线，这也是它比 CRAG／Agentic 落地更重的原因

### `06b-rag#21` · 漏了必答点 · 把握 high

**题**：RAG 中的文档是怎么存的？粒度是多大？详细说说文档切割（Chunking）

- 我们：粒度没有标准答案，要按文档类型选策略；三个约束一起看
- 原文：通常 500～1000 token 是合理的起点
- 问题：题目直接问「粒度是多大」，我们全篇只讲方法论和约束，没给任何可落地的量级数字，面试时等于没回答这一问
- 建议：在「粒度怎么定」开头先给一个常用起点量级（几百到一千 token 级别），再接现有的三条约束和「要靠评测调」的结论

### `06b-rag#22` · 漏了必答点 · 把握 medium

**题**：怎么规避语义被切割掉的问题？

- 我们：离线用小模型为每个 chunk 生成一句「它在讲什么、属于什么背景」的说明
- 原文：让模型看着整篇原始文档，为每个 chunk 生成背景说明
- 问题：这一招（Contextual Retrieval）的关键前提是生成时要把整篇文档一起喂给模型；我们没写清输入是什么，照我们的描述实现会变成「只看 chunk 生成说明」，补不回任何缺失语境
- 建议：把这条改成「输入 = 整篇文档 + 该 chunk，让模型输出这个 chunk 在全文中的位置与主题」，并顺带点名它就是 Contextual Retrieval

### `06b-rag#25` · 漏了必答点 · 把握 high

**题**：什么是向量数据库？有没有做过向量数据库的对比选型？

- 我们：避免对具体产品下断言，说形态和判据比说名字安全也专业
- 原文：按规模选 Chroma / Qdrant / Milvus / pgvector
- 问题：题目后半句就是问「对比选型」，而我们刻意不提任何产品名，导致这张卡回答不了它自己的题干——只有判据没有候选，没法构成一次选型
- 建议：保留形态与判据框架，但补一张最小候选表（嵌入式、自托管专用库、分布式、托管云、关系库插件各点一个代表），只写适用规模与形态、不排名次

### `06c-agent#14` · 与原文冲突 · 把握 medium

**题**：ReAct、Plan-and-Execute、Reflection 三种范式有什么核心区别

- 我们：Reflection 成本「翻倍以上」；每加一层范式，调用次数和成本「至少翻一倍」
- 原文：token 消耗在基础范式上再增加 30% 到 100%
- 问题：反思的成本增幅被我们说高了：原文给的是 +30%~100%（1.3~2 倍），我们写成「至少翻一倍」。更要紧的是「每加一层范式都至少翻一倍」这个泛化不成立——Plan-and-Execute 相比 ReAct 其实是省 token 的（原文算例 14500 vs 30000），我们自己的表格也写了它「通常更可控」，前后自相矛盾。
- 建议：把成本这句收窄到只讲 Reflection，改成「每轮反思至少多一次调用，通常在基础成本上多三成到一倍」；删掉「每加一层范式成本至少翻一倍」的泛化，或改成「每加一层都要重新算成本，不能默认更贵也不能默认更省」。

### `06c-agent#6` · 漏了必答点 · 把握 high

**题**：如何赋予 LLM 规划能力？

- 我们：分六层：提示层/结构层/反馈层/工具层/搜索层/知识层 SOP
- 原文：CoT、ToT、GoT 层层递进，是这题主线
- 问题：整卡没出现 CoT / ToT / GoT 任何一个名词，而这题面试官通常就顺着这条演进链追问（CoT 单链走错就全错 → ToT 边探索边评估边剪枝 → GoT 图结构中间结论可合并复用）；我们的「搜索层」讲的其实就是 ToT，却没点名
- 建议：开头补一段经典三级演进并点名 CoT/ToT/GoT，把搜索层标注成 ToT 思想（强调是边探索边剪枝、不是最后才选），再接我们自己的工程六层

### `06d-tool-calling-mcp#11` · 漏了必答点 · 把握 high

**题**：MCP 由哪几部分组成？

- 我们：概念上是三个角色——Host / Client / Server
- 原文：三层：角色层、能力层、协议层
- 问题：题目问「由哪几部分组成」，我们只给了角色层 + 能力层两层，协议层（JSON-RPC 2.0 消息格式 + stdio / Streamable HTTP 传输，且两者解耦）没有作为一个组成部分列出来，只在图里以「stdio」「HTTP 流式」的注脚出现。原文里这正是被面试官点名漏掉的那一层。
- 建议：把答案结构改成三层：角色层（Host/Client/Server）、能力层（Tools/Resources/Prompts）、协议层（JSON-RPC 2.0 + stdio/Streamable HTTP，消息格式与传输解耦），协议层单独起一节，和现有工程注意事项并列。

### `06d-tool-calling-mcp#3` · 漏了必答点 · 把握 high

**题**：MCP 协议通常采用什么通信方式？

- 我们：远程服务走基于 HTTP 的流式传输；JSON-RPC 风格
- 原文：远程用 Streamable HTTP，消息 JSON-RPC 2.0
- 问题：这题问的就是「叫什么通信方式」，我们两个关键名词都做了模糊化：远程传输没说出 Streamable HTTP（以及早期 HTTP+SSE 双端点已被规范标记弃用、单端点内部仍走 SSE），消息格式只说「JSON-RPC 风格」而不是 JSON-RPC 2.0。按这张卡答，面试官会觉得没真看过规范。
- 建议：把远程传输的名字写实为 Streamable HTTP，并补一句「早期 HTTP+SSE 双端点方案已被标记弃用、保留兼容」；消息格式改成 JSON-RPC 2.0，并强调「消息格式与传输方式解耦」这条设计点，「以官方规范为准」的提醒保留。

### `06d-tool-calling-mcp#8` · 漏了必答点 · 把握 high

**题**：LLM 是如何学会调用外部工具的？

- 我们：后训练用带工具调用轨迹的数据专门教它…
- 原文：SFT 教会怎么调，RLHF 教会什么时候调
- 问题：整张卡把训练环节统称「后训练」，从头到尾没出现 SFT、RLHF 这两个名词，也没讲「两阶段分工」这条主线。而原文这题的追问点恰恰是「只说 SFT 不提 RLHF 要扣分」，我们连 SFT 都没点名，会答不出阶段划分。
- 建议：把第 ② 步拆成两句：SFT 用完整工具调用轨迹样本解决「会不会调、格式对不对」，之后的偏好优化/强化（RLHF 或用可验证奖励的变体，低成本版是 RLAIF）解决「该不该调」的边界感；负样本那条挂到第二阶段下面。

### `06e-langchain#0` · 漏了必答点 · 把握 medium

**题**：你了解过哪些 AI Agent 开发框架？

- 我们：通用编排类（LangChain / LangGraph、LlamaIndex 的工作流）
- 原文：优势集中在数据接入、文档解析、索引和检索
- 问题：四类划分里 LlamaIndex 只作为「通用编排类」的例子出现，全篇没提它最有辨识度的数据接入/解析/索引/检索定位，正好落进原文点名的「把它当成另一个 LangChain」误区
- 建议：单列一类「数据与检索侧框架」，写清 LlamaIndex 的数据链路优势和企业知识库、复杂 RAG 这类适用场景

### `06e-langchain#1` · 漏了必答点 · 把握 medium

**题**：使用 LangChain 构建 Agent 的核心步骤是什么？

- 我们：本质五步：接模型 → 定义工具 → 写提示 → 装配循环 → 加记忆护栏
- 原文：构建 Agent 的第一步不是选择模型，而是定义任务
- 问题：起手就是接模型，缺「先定义任务边界：能做什么、不能做什么、什么算成功、何时停止或转人工」这一步；另外完全没提结构化输出，结果要进前端/工单/下游流程时会漏掉这层约束
- 建议：在最前面加一步任务边界定义，并在提示那步补上「结果要给程序消费时用结构化输出固定字段与类型」

### `06e-langchain#10` · 漏了必答点 · 把握 medium

**题**：LangChain 如何实现短期记忆和长期记忆？

- 我们：短期=每次把历史或摘要重新拼进上下文；长期=写外部存储再检索少量
- 原文：短期是线程级状态，由 Checkpointer 按 thread_id 保存
- 问题：题目问的是「LangChain 如何实现」，答案只有通用记忆工程策略，没给出框架里的落地机制和作用域划分（线程内快照 vs 跨线程存储），而这正是原文说最容易混用、混用后会换会话失忆或串用户的点
- 建议：在开头补一行机制映射：线程内状态按会话 ID 存快照，跨线程记忆按「租户/用户/记忆类型」命名空间存，二者作用域不同不能互相替代

### `06e-langchain#11` · 漏了必答点 · 把握 high

**题**：在 LangChain 中，如何为 Agent 注册工具？

- 我们：注册工具本质就是提供名字、描述、参数结构、执行函数（全篇不出现任何 LangChain API）
- 原文：四种方式：普通函数 / @tool / StructuredTool / BaseTool，用 create_agent(tools=[...]) 注册
- 问题：题目问的是「在 LangChain 中」怎么注册，原文的核心考点是四种定义方式的选择边界和 create_agent 注册入口，我们的答案完全框架无关，被追问「@tool 和 BaseTool 什么时候用」会答不上来
- 建议：在开头补一小段 LangChain 具体做法：带类型注解和 docstring 的普通函数可直接放进 tools；大多数业务工具用装饰器方式自定义名称/描述/参数模型；需要运行时组装同步异步实现时用 StructuredTool；工具要长期持有客户端、管生命周期时才继承 BaseTool；最后统一放进 create_agent 的 tools 列表

### `06e-langchain#2` · 漏了必答点 · 把握 medium

**题**：请你谈谈对 LangChain 中核心概念「Chain」的理解，以及它的核心作用

- 我们：早期一堆预置的「XX 任务链」类，后来转向通用可组合原语 + 管道式拼装
- 原文：Chain 最重要的技术基础是 Runnable
- 问题：把演进讲对了但全程没落到名词：没有 Runnable、没有 LCEL，也没说 LLMChain / SequentialChain 已经在 v1 里被移进 langchain-classic，面试官一追问「Chain、Runnable、LCEL 什么关系」就接不上
- 建议：在「统一接口」那段点名 Runnable，在演进那段点名 LCEL 与管道符，并补一句旧式 Chain 现在归在哪个包、只用于维护存量项目

### `06e-langchain#3` · 漏了必答点 · 把握 medium

**题**：Deep Research 的实现逻辑和适用场景是什么？

- 我们：① 澄清 ② 规划 ③ 并行检索 ④ 抽取 ⑤ 反思补搜 ⑥ 分节撰写
- 原文：Supervisor 拆分子课题，Researcher 并行检索
- 问题：流程画成了单个 Agent 的大循环，缺了参考实现的主架构：主控拆题、多个子 Agent 在各自隔离的上下文里搜证并只回传压缩证据，以及「并行搜证据、统一写报告」而不是并行写章节的理由
- 建议：把规划和检索两步改写成主控派发子课题、子 Agent 独立上下文搜证回传，并说明子 Agent 的首要价值是上下文隔离、并行只是副产品

### `06e-langchain#4` · 漏了必答点 · 把握 high

**题**：请你谈谈 LangChain4j 这类 Java 生态的 LangChain 衍生框架，主要帮开发

- 我们：把模型接入、提示、工具、RAG、记忆用 Java 的习惯重新实现一遍
- 原文：它不是 LangChain 官方的 Java 移植版
- 问题：缺了这道题最该先说的定位纠偏：它是独立设计、API 与发布节奏都独立于 Python LangChain 的框架，不是官方 Java 版；答成「衍生/移植」正是原文里被面试官第一句就否掉的说法
- 建议：开头先纠偏定位（名字受启发但独立设计、面向 Java 习惯），再讲它解决的语言错位问题

### `06e-langchain#5` · 漏了必答点 · 把握 medium

**题**：LangChain 的底层架构与实现原理是什么？

- 我们：④ 编排层：链式编排（顺序流水线）／图式编排（状态·循环·检查点·人在环）
- 原文：create_agent 会把模型节点和工具节点编译成状态图
- 问题：架构题把链式和图式并列成两个可选编排，没讲 v1 的主线是「标准 Agent 本身就是编译出来的图」，也没提 State / Context / Store 的数据分层和 Middleware 这个横切扩展点
- 建议：在编排层写清 Agent 运行时就是图运行时，并补一小节讲可变状态、可信上下文、跨线程存储三类数据的分工，以及中间件挂在模型/工具调用前后

### `06e-langchain#6` · 漏了必答点 · 把握 medium

**题**：LangChain 框架面试题介绍

- 我们：容易扣分的坑之一：背类名和版本号，改过多轮，说错反而暴露
- 原文：拿旧版的 AgentExecutor 回答 v1 的问题
- 问题：考点地图漏掉「大版本升级 / v1 边界」这一整块（原文有一整道题在考），而且「别记版本」的建议会让人正好踩中原文点名的翻车点——用旧写法回答新版本问题
- 建议：加一个「版本边界」考点：知道哪些抽象已是 legacy、v1 主线是什么；保留「不背构造参数」的分寸，但要求能说清新旧分界

### `06e-langchain#9` · 漏了必答点 · 把握 high

**题**：LangGraph 相比于 LangChain 有哪些核心优势？更适配哪些 Agent 场景？

- 我们：在链里要自己套 while 并手工管状态（把 LangChain 当成「链」来比）
- 原文：优势不是「LangChain 没有这些能力」
- 问题：整张卡把 LangGraph 的能力讲成 LangChain 所没有的，缺了这道题必须先说的前提：两者是上下层关系，标准 Agent 本来就跑在 LangGraph 上，优势在于控制权下沉而非独占功能
- 建议：开头补一句层级关系，再把每条优势重述成「你能自己决定它放在哪个节点、围绕哪些状态生效」，而不是「只有图才有」

### `10-golang#114` · 我们写错了 · 把握 high

**题**：9.6 GMP能不能去掉P层？会怎么样？

- 我们：容器里 GOMAXPROCS 默认读宿主机核数，要用 automaxprocs 修正
- 原文：（原文未涉及这一点）
- 问题：已经过时：Go 1.25 起运行时默认按 cgroup 的 CPU 带宽限额设置 GOMAXPROCS，并会在限额变化时动态更新（主模块 go 指令 ≥ go1.25 时生效），automaxprocs 只对更老的版本才必需
- 建议：把这句改成分版本表述：老版本只看宿主机核数、需要 automaxprocs；新版本运行时已自动感知 cgroup CPU 限额

### `10-golang#115` · 我们写错了 · 把握 high

**题**：9.7 P和M在什么时候会被创建？

- 我们：容器里默认读到的是宿主机核数而非 cgroup 限额，生产上用 automaxprocs
- 原文：（原文未涉及这一点）
- 问题：同上，Go 1.25 之后运行时默认已按 cgroup CPU 限额决定 GOMAXPROCS，这条注意事项按现在的版本讲是错的
- 建议：改成分版本表述，说明新版本运行时自带 cgroup 感知，automaxprocs 是老版本的兜底手段

### `10-golang#120` · 我们写错了 · 把握 high

**题**：10.2 知道 golang 的内存逃逸吗？什么情况下会发生内存逃逸？

- 我们：func p() []byte { return make([]byte, 64) } 常量且小 → 可能不逃逸
- 原文：切片容量超出编译期确定范围时会逃逸
- 问题：这个对照例把切片 return 出去了，不管长度是不是常量都必定逃逸；「常量小尺寸可以留在栈上」的前提是不逃出函数，例子和结论对不上，容易让人以为返回值也能栈分配
- 建议：把对照例换成只在函数内部使用、不外传的 buf := make([]byte, 64)，再和变量长度那行做对比

### `10-golang#134` · 我们写错了 · 把握 high

**题**：11.10 GC触发的时机有哪些？

- 我们：GOGC=off 关闭自动 GC，只剩 2 分钟强制那一条和手动触发
- 原文：超过两分钟没产生任何 GC，强制触发 GC
- 问题：定时触发在 GOGC 为负（off）时会直接判假，所以 GOGC=off 时那条 2 分钟强制 GC 同样失效，真正剩下的只有触到 GOMEMLIMIT 和手动 runtime.GC()。这条还直接影响后面「GOGC=off + GOMEMLIMIT」那个推荐配置的理解
- 建议：改成「GOGC=off 时定时强制 GC 一并失效，只有内存逼近 GOMEMLIMIT 或手动调用才会触发」

### `10-golang#140` · 我们写错了 · 把握 high

**题**：12.2 三个goroutinue交替打印abc 10次

- 我们：一旦改成有缓冲，发送方就能连发几次不阻塞，交替性立刻被破坏
- 原文：（原文只给代码，未讨论缓冲）
- 问题：交替性来自「令牌唯一」而非「channel 无缓冲」：每个协程一轮只在收到令牌后发一次，永远不可能连发，三个 channel 全加缓冲顺序照样正确；而且我们自己的示例代码就把 chs[0] 设成了容量 1 的缓冲 channel，前后自相矛盾（同 deck 的 12.5 卡对这一点的解释才是对的——单个共享 channel 加缓冲才会自收自发）
- 建议：改成「无缓冲让每次交接同时是同步点，语义最干净；但顺序的根本保证是令牌唯一。每人一个专属 channel 时加缓冲不破坏交替，只有单个共享 channel 加缓冲才会自己取回令牌」

### `10-golang#142` · 我们写错了 · 把握 high

**题**：12.4 两个协程交替打印奇偶数

- 我们：用有缓冲 channel → 发送方不阻塞，可以连发多次，交替性被破坏
- 原文：（原文只给代码，未讨论缓冲）
- 问题：本卡是 odd/even 两个专属 channel 的乒乓结构，每个协程只在收到对方令牌后才发一次，加缓冲也不可能连发，交替性依然成立；「面试官问改成有缓冲会怎样，答案是交替性被破坏」这句会答错
- 建议：把结论限定住：单个共享 channel 才靠无缓冲防止自收自发；两个专属 channel 的结构里，加缓冲只影响最后一棒是否阻塞，不影响交替顺序

### `10-golang#146` · 我们写错了 · 把握 high

**题**：12.8 使用两个Goroutine，向标准输出中按顺序按顺序交替打出字母与数字

- 我们：为什么必须无缓冲……改成有缓冲，发送方不阻塞，就可能连打两个字母
- 原文：（原文只给代码，未讨论缓冲）
- 问题：字母协程只能从 letterCh 收到令牌后才打印，letterCh 的令牌只来自 main 和数字协程，所以即使两个 channel 都有缓冲也不可能连打两个字母；「必须无缓冲」这条因果讲错了
- 建议：把「必须无缓冲」改成「无缓冲让交接同时建立 happens-before，最干净；顺序的根本保证是令牌唯一 + 收发次数配对」，并去掉「有缓冲就会连打两个字母」的推断

### `10-golang#57` · 与原文冲突 · 把握 high

**题**：2.2 Go语言里slice是怎么扩容的？

- 我们：阈值历史上是 1024 个元素，后来改成按字节数判断
- 原文：原容量小于 256 翻倍，超过 256 用新公式
- 问题：Go 1.18 起的增长因子切换阈值仍然是元素个数（256 个 oldcap），不是字节数；1024 是 1.17 及以前的旧阈值。字节只出现在最后 roundupsize 对齐 size class 那一步
- 建议：把「后来改成按字节数判断」改成「Go 1.18 起阈值从 1024 降到 256 个元素，超过后按 newcap += (newcap+3*256)/4 平滑逼近 1.25 倍」，并把「向上对齐 size class」单独说成字节层面的事

### `11-data-structures-algorithms#23` · 我们写错了 · 把握 high

**题**：介绍一下数据结构中的栈？怎么用 java 实现？

- 我们：需要线程安全时用 ConcurrentLinkedDeque，或者 Collections.synchronizedDeque()
- 原文：原文只说内置 Stack 继承 Vector、多线程下不够高效
- 问题：JDK 的 Collections 里根本没有 synchronizedDeque() 这个方法（只有 synchronizedList/Set/Map/Collection/SortedXxx/NavigableXxx），照抄会编译不过
- 建议：把这个方法名换成真实存在的选项：ConcurrentLinkedDeque（非阻塞并发）、LinkedBlockingDeque（阻塞有界），或退一步写 Collections.synchronizedList(new LinkedList<>()) 并说明它不是 Deque 视图

### `14-operating-system#46` · 漏了必答点 · 把握 high

**题**：进程调度算法有哪些？

- 我们：列了 FCFS、SJF、SRTF、优先级、RR、多级队列、MLFQ、CFS、EDF
- 原文：高响应比优先（HRRN）权衡了短作业和长作业
- 问题：这是一道枚举题，原文单独讲了「高响应比优先 HRRN」（响应比=(等待+服务)/服务，随等待时间自动提升，兼顾长短作业），我们整张卡完全没提；HRRN 是国内 408/面试的经典必答项，漏掉会显得答案不全
- 建议：在算法对比表里补一行 HRRN：讲清响应比公式、它同时照顾短作业和长作业、以及它是非抢占的；顺带点出它和「优先级+老化」是同一思路的两种表达

### `15-computer-network#59` · 原文有问题（我们对） · 把握 high

**题**：token，session，cookie的区别？

- 我们：本卡没跟着说「加密」；同 deck 的 63 卡明确写了 JWT 是签名不是加密
- 原文：用户信息都被加密到token中，服务器收到token后解密
- 问题：原文把 JWT 说成加密/解密，实际是 Base64URL 编码 + 签名，任何人都能解出 Payload，服务端做的是验签
- 建议：不要按原文改；反而在 Token 那一列补一句「令牌内容是编码不是加密，签名只防篡改不防偷看」，跟 63 卡口径统一

### `15-computer-network#61` · 原文有问题（我们对） · 把握 medium

**题**：如果我把数据存储到 localStorage，和Cookie有什么区别

- 我们：HttpOnly 是 Cookie 独有的护城河，localStorage 没有等价机制，XSS 能一次性读走
- 原文：LocalStorage 的数据……相对而言更安全一些
- 问题：原文笼统说 localStorage 比 Cookie「更安全」，只看了「不随请求自动发送」这一面，忽略了它没有 HttpOnly、对 XSS 完全裸奔；存凭证时结论正相反
- 建议：保持我们的写法；真要贴原文只能补「在不自动发送这点上 localStorage 少了 CSRF 和链路暴露风险」，绝不能改成「localStorage 更安全」

### `15-computer-network#74` · 我们写错了 · 把握 high

**题**：TCP 三次握手，客户端第三次发送的确认包丢失

- 我们：重传耗尽后「放弃这个半连接、从半连接队列移除」，时序图紧接着画 S→C 发送 RST
- 原文：已达到最大重传次数……服务端就会断开连接
- 问题：SYN+ACK 重传耗尽时内核是静默丢弃这条半连接、不会主动发 RST；RST 要等客户端后来真发数据、报文打到监听套接字上才回。开头一句话、时序图、关键点第 4 条三处都写成了「放弃后就发 RST」，而第 4 条后半句自己又说「直到它真的去发数据才会收到 RST」，前后打架
- 建议：把时序图那一步改成「静默丢弃半连接、不发任何报文」，RST 挪到客户端后续发数据之后；开头一句话和关键点第 4 条同步改成「服务端悄悄放弃，客户端要到自己发数据时才被 RST 打醒」

### `15-computer-network#78` · 我们写错了 · 把握 high

**题**：服务器收到第一个 SYN 报文，回复的 SYN + ACK 报

- 我们：三种丢包场景对照表第③行：「服务端先放弃并发 RST，客户端发数据时才知道」
- 原文：重传达上限后服务端就会断开连接
- 问题：跟 74 卡同一个错：服务端重传耗尽是静默丢弃半连接，不会主动发 RST，RST 是客户端后来发数据才被回的，两件事的先后和触发条件被压成一句说反了
- 建议：把第③行改成「服务端静默放弃、不发报文；客户端要到自己发数据收到 RST 才发现」，跟 74 卡改法保持一致

### `15-computer-network#84` · 与原文冲突 · 把握 medium

**题**：第三次挥手一直没发，会发生什么？

- 我们：表格「主动方（FIN_WAIT_2）还能收数据吗：能（对方发送方向还开着）」
- 原文：close 关闭的孤儿连接「无法再发送和接收数据」
- 问题：同一张表另一行按 close() 出来的孤儿连接讲超时兜底，这一行却按 shutdown 半关闭讲还能收数据；close() 后内核置了 RCV_SHUTDOWN，FIN_WAIT_2 再收到数据直接回 RST，本 deck 的 85 卡也是这么写的
- 建议：把这一行拆成两种情况：shutdown(SHUT_WR) 半关闭时能继续收；close() 出来的孤儿连接收到数据内核直接回 RST，应用读不到

### `15-computer-network#84` · 漏了必答点 · 把握 high

**题**：第三次挥手一直没发，会发生什么？

- 我们：FIN_WAIT_2 通常有一个系统级的超时兜底 / 孤儿连接有系统级超时
- 原文：tcp_fin_timeout 控制该状态时长，默认值是 60 秒
- 问题：全卡只说「有个系统级超时」，既没给参数名也没给数值，读者判断不出这个兜底是 60 秒还是几小时，而这正是这题被追问的点
- 建议：在 FIN_WAIT_2 那段补一句：close() 出来的孤儿连接由 tcp_fin_timeout 兜底、Linux 默认 60 秒；shutdown 半关闭且 fd 仍被持有的连接不受它限制

### `16-database#42` · 原文有问题（我们对） · 把握 high

**题**：mysql中的一些基本函数，你知道哪些？

- 我们：LENGTH 返回字节数，CHAR_LENGTH 返回字符数；utf8mb4 下 '中文' 是 6 和 2
- 原文：LENGTH(str)：返回字符串的长度（字符数）
- 问题：原文把 LENGTH 说成返回字符数，实际返回的是字节数，字符数要用 CHAR_LENGTH；我们是对的，是原文错了
- 建议：维持我们的写法，别按原文改；可以在函数表里额外标一句「原文此处有误」防止后续被回改

### `17-redis-cache#33` · 原文有问题（我们对） · 把握 medium

**题**：跳表是怎么设置层高的？

- 我们：最大层数是固定编译期常量（历史上是 32 层）
- 原文：层高最大限制是 64
- 问题：Redis 源码里 ZSKIPLIST_MAXLEVEL 是 32（注释写明足够支撑 2^64 个元素，因为 p=1/4、4^32=2^64），原文的 64 是错的；我们写 32 是对的
- 建议：我们这张卡不用改，可以在「面试加分点」补一句：网上（含小林原文）常见 64 的说法，实际源码常量是 32，被追问时说清楚出处即可

### `17-redis-cache#52` · 原文有问题（我们对） · 把握 high

**题**：哨兵机制的选主节点的算法介绍一下

- 我们：replica-priority 优先级【数值小的优先】；配成 0 表示永不当主
- 原文：选择优先级 slave-priority 最大的从节点作为主节点
- 问题：Redis 的 replica-priority/slave-priority 是「数值越小越优先」，0 表示永不被提升；原文说选最大的，方向反了。我们是对的
- 建议：我们这张卡不用改，建议在卡里显式点一句「注意不少资料写成『优先级最大者当选』，是反的」，防止用户对照原文时被带偏

### `18-linux-troubleshooting#34` · 我们写错了 · 把握 high

**题**：修改文件权限用什么命令？

- 我们：新文件权限 = 666 - umask，新目录 = 777 - umask
- 原文：（原文未涉及 umask，这是我们自己补的）
- 问题：umask 是按位清除（AND NOT）不是十进制/八进制减法；卡片上文刚举了 umask 027，按减法算 666-027=637 是错的，正确结果是 640（目录 750）
- 建议：把公式改成「用 umask 里置位的权限位去清掉 666/777 对应的位」，并补一个 umask 027 → 文件 640、目录 750 的例子，说明减法只在 022 这类不产生借位的情况下凑巧成立

### `19a-git#19` · 我们写错了 · 把握 medium

**题**：什么是 Fast-Forward 合并？和普通 merge 有什么区别？

- 我们：当目标分支的提交完全包含在当前分支的历史里时…只要把分支指针往前挪
- 原文：main 从 feat/a 分出去之后没有新的 commit
- 问题：开头这句话的主客体反了：能快进的条件是「当前分支（main）的最新提交是被合并分支的祖先」，而不是「被合并分支的提交已包含在当前分支里」（那是 Already up to date）。下面的 mermaid 图反而画对了，只有首句错
- 建议：首句改成「当前分支自分叉后没有新提交、它的最新提交是被合并分支的祖先时，Git 只需把指针前移」，和下面的图保持一致

### `20b-distributed#17` · 与原文冲突 · 把握 medium

**题**：分布式事务的解决方案你知道哪些？

- 我们：把 TCC 放进「强一致（同步阻塞）」分支，和 2PC/XA 并列
- 原文：TCC 的一致性列为「最终一致性」
- 问题：TCC 是补偿型柔性事务：Try 之后各分支存在 Confirm 未全部完成的窗口，业界（含 Seata 文档）都归为最终一致，而且它恰恰不是同步阻塞型；我们把它划到强一致分支了
- 建议：把 TCC 从强一致分支挪到柔性/最终一致一侧，标成「补偿型，最终一致但一致窗口很短、性能好」，强一致分支只留 2PC/XA

### `20c-message-queue#24` · 我们写错了 · 把握 medium

**题**：RocketMQ怎么保证消息不被重复消费？

- 我们：msgId / offsetMsgId 由 Broker 生成；生产端重试会产生新消息，msgId 不同
- 原文：原文未提及此点（只说用唯一订单号做幂等标识）
- 问题：RocketMQ 里 msgId（UNIQ_KEY）是生产者客户端在发送前生成的，SDK 内部重试时保持不变；只有 offsetMsgId 才由 Broker 按 CommitLog 物理位置生成、每次写入都变。卡片把两个 ID 混成一个，并给了「重试后 msgId 会变」这个错误理由，而这段被自己标成「这题的关键考点」。
- 建议：把两个 ID 拆开讲：msgId/UNIQ_KEY 是客户端生成、SDK 重试时不变，但 Broker 不对它做任何唯一约束或去重，所以照样不能当去重键；offsetMsgId 是物理位置，重投一定变。最终结论（用业务键 + 数据库唯一约束）不用改，只改理由。


---

## 轻微（用词/边界） · 83 条

### `02-test-design-process#109` · 漏了必答点 · 把握 medium

**题**：4. H5页面性能测试关注哪些核心指标？

- 我们：阈值不该背书上的数字，按自己业务的历史数据定基线
- 原文：LCP 应小于2.5秒、FID 小于100毫秒、CLS 小于0.1
- 问题：Core Web Vitals 的三个「良好」阈值是官方标准值、面试常被直接追问，卡片一个数字都没给还劝人别记，容易让人答不上来
- 建议：保留「按业务定基线」的观点，但先把官方 good 阈值列出来（LCP 2.5s / CLS 0.1 / INP 200ms），再说明业务基线是在此之上的自定要求

### `02-test-design-process#123` · 原文有问题（我们对） · 把握 high

**题**：18. 如何测试H5页面在微信浏览器中的兼容性？

- 我们：iOS 和 Android 上底层不一样，Android 侧历史上还用过腾讯自研内核
- 原文：微信浏览器（X5内核）是一个必须重点测试的特殊环境
- 问题：原文把微信内置浏览器一律说成 X5 内核，这在 iOS 上根本不成立（受平台限制只能用系统 WebKit），Android 侧近年也已切到新的自研内核；我们的写法是对的
- 建议：不用改我们的卡；后续若做对齐，注意别按原文把微信内核统一改写成 X5

### `02-test-design-process#136` · 漏了必答点 · 把握 medium

**题**：11. 解释一下小程序的分包加载机制，测试时需要注意什么？

- 我们：平台对主包和总包都有体积上限，具体数值以官方最新规定为准
- 原文：每个子包的大小限制（目前为2M）
- 问题：主包/单个分包 2MB、全部分包合计 20MB 是分包这题最常被直接问的硬数字，卡片全部回避了
- 建议：在体积限制那一行补上当前数值（单包 2MB、总量 20MB 量级），后面再加一句以官方文档为准即可

### `02-test-design-process#141` · 与原文冲突 · 把握 high

**题**：16. 什么是Minium？它有什么特点？

- 我们：用 Python 写用例……Python 生态成熟，和 pytest、报告工具、CI 结合顺畅
- 原文：支持Python和JavaScript
- 问题：Minium 官方同时提供 Python 和 JavaScript 两套 SDK，卡片只写 Python，问到「支持什么语言」会答漏
- 建议：开头改成官方提供 Python / JavaScript 两种语言版本，再说明团队通常选 Python 是因为生态和 pytest 更顺手

### `02-test-design-process#149` · 我们写错了 · 把握 high

**题**：4. 请解释HTTP请求的组成。

- 我们：Content-Length: 46，配的 body 是 {"skuId":1001,"count":2,"couponId":null}
- 原文：（原文只给组成，未给示例报文）
- 问题：示例里的 body 实际是 40 字节，标的却是 46；同一张卡下面又专门说「Content-Length 和实际长度对不上服务端会一直等」，自相矛盾
- 建议：把示例头里的长度改成与 body 真实字节数一致（40），或者干脆写成占位说明避免再对不上

### `02-test-design-process#150` · 我们写错了 · 把握 high

**题**：5. 请解释HTTP响应的组成。

- 我们：Content-Length: 62，配的 body 是 {"orderId":98765,"status":"CREATED","amount":199}
- 原文：（原文只给组成，未给示例报文）
- 问题：示例 body 实际 49 字节，头里写 62，学员照着数会对不上
- 建议：把示例的 Content-Length 改成 body 的真实字节数

### `02-test-design-process#151` · 我们写错了 · 把握 high

**题**：6. 请列出常见的HTTP方法及其用途。

- 我们：跨域请求会先发一个 OPTIONS 预检，要确认预检本身不被鉴权拦住
- 原文：（原文未提预检）
- 问题：只有非简单请求才触发预检；简单请求（GET/HEAD、或 form/text 类 Content-Type 且无自定义头的 POST）是直接发出去的，写成「跨域请求会先发预检」是过度概括
- 建议：补一句触发条件：带自定义头、用 application/json 等非简单 Content-Type、或用 PUT/DELETE 等方法时才先发 OPTIONS 预检

### `02-test-design-process#163` · 我们写错了 · 把握 medium

**题**：18. 在Postman中，如何使用Collection Runner？

- 我们：Runner 不是压测工具。它是串行的
- 原文：原文只讲批量运行，未提性能
- 问题：新版 Postman 的 Collection Runner 已经内置性能测试模式，可以按虚拟用户数并发跑集合，"它是串行的、不能压测"这句已经过时。
- 建议：改成"Runner 的功能测试模式是串行的；虽然新版带了性能测试模式，但正经压测仍建议用专门的性能工具"。

### `02-test-design-process#31` · 我们写错了 · 把握 high

**题**：2. 黑盒测试和白盒测试有什么区别？

- 我们：覆盖强度有层级：语句覆盖 < 判定（分支）覆盖 < 条件覆盖 < 判定条件覆盖 < 路径覆盖
- 原文：原文只列「代码覆盖、路径测试」
- 问题：条件覆盖并不包含判定覆盖，两者互不包含，写成严格递增的「<」链是经典的说法错误，而这条恰好被我们当成加分点在教
- 建议：把判定覆盖与条件覆盖并列，另加一句「条件覆盖 100% 也可能判定覆盖不到 100%」，从判定条件覆盖开始才恢复递增关系

### `02-test-design-process#74` · 原文有问题（我们对） · 把握 high

**题**：14. Web性能测试关注哪些核心指标？

- 我们：INP 是 Core Web Vitals 中取代 FID 的响应性指标
- 原文：首次输入延迟（FID）：衡量交互体验
- 问题：原文把 FID 当成现行核心指标，它已被 INP 取代并从 Core Web Vitals 中移除；我们的写法是对的，问题在原文
- 建议：无需改我们的卡，保持 INP 为主、FID 标注为已废弃的历史指标即可

### `02-test-design-process#93` · 我们写错了 · 把握 high

**题**：17. 弱网测试的关注点是什么？

- 我们：小标题「弱网要拆成三种」，下面表格实际列了高延迟、高丢包、低带宽、抖动四行
- 原文：原文未做分类
- 问题：标题的数量和表格内容对不上，同一张卡后面的加分点又说「拆成延迟/丢包/带宽/抖动四种」，前后自相矛盾
- 建议：把小标题改成四种，与表格四行和加分点里的说法统一

### `02-test-design-process#94` · 我们写错了 · 把握 medium

**题**：18. 如何模拟弱网环境进行测试？

- 我们：做了 SSL Pinning 的 App 根本不走你的代理，你以为在测弱网，其实测的是「无网」
- 原文：设置带宽、延迟、丢包率后手机请求都经过 Charles
- 问题：机制说反了：Pinning 的 App 照样经代理走 CONNECT 隧道，限速依然生效；只有对该域名开启 SSL 解密拦截时握手才会失败，那时才退化成「无网」
- 建议：改成「对 Pinning 域名开启 SSL 解密就会握手失败，此时应关闭对该域名的解密、只做限速」，并把「不走代理」这条留给不读系统代理的长连接与 SDK

### `03b-api-automation#12` · 原文有问题（我们对） · 把握 medium

**题**：9. 断言怎么写？用什么工具？

- 我们：Java 侧常用 Hamcrest 匹配器（RestAssured 内建）或取出对象后用 AssertJ
- 原文：Hamcrest是TestNG推荐的
- 问题：原文错：Hamcrest 与 TestNG 无绑定关系，它随 JUnit4 / RestAssured 提供；TestNG 自带的是 Assert 与 SoftAssert。我们的写法是对的
- 建议：保持现状，后续修订不要按原文回填「TestNG 推荐 Hamcrest」

### `03b-api-automation#13` · 我们写错了 · 把握 medium

**题**：10. 如何处理接口加密和签名？

- 我们：requests：用自定义 auth 可调用对象，或 Session 的请求钩子
- 原文：（原文只讲工具类封装，未涉及）
- 问题：requests 的 hooks 机制只有 response 一种事件，没有请求钩子；注册所谓 request 钩子会被静默忽略，签名根本不会被加上
- 建议：把「请求钩子」删掉，改成「自定义 auth 对象，或自己继承 Session 重写 prepare_request」；response 钩子只在解密/验签时提

### `03b-api-automation#21` · 原文有问题（我们对） · 把握 medium

**题**：18. 为什么选Java而不是Python做接口自动化？

- 我们：语言跟着被测系统和团队走；同一套分层在两边同构
- 原文：Java执行效率高，500条用例跑5分钟，Python要10分钟
- 问题：原文这个倍数站不住：接口用例耗时由网络 IO 和服务端响应决定，语言差异不会造成 2 倍差距。我们没抄这条，方向是对的
- 建议：保持现状；若要谈效率，只说并发模型差异（线程 vs GIL），不要引用执行时间倍数

### `03b-api-automation#30` · 我们写错了 · 把握 medium

**题**：7. 如何实现统一的断言机制？

- 我们：actual = jsonpath(resp.json(), path)；assert actual == expected
- 原文：jsonpath(response.json(), json_path)[0]
- 问题：jsonpath() 返回的是匹配结果列表（无匹配时返回 False），直接拿它和标量 expected 比永远不相等，这段封装照抄就跑不通
- 建议：取值后先判空再取第 0 个元素，取不到时抛「路径未匹配到」的明确错误，再做相等比较

### `03b-api-automation#31` · 我们写错了 · 把握 high

**题**：8. Pytest的fixture在框架中如何应用？

- 我们：别在 fixture 里写断言…前置校验失败应该抛错（会被标为 error 而不是 failed）
- 原文：（原文未涉及）
- 问题：fixture 里 assert 失败同样被 pytest 记为 error 而不是 failed，用「error vs failed」来区分断言和抛异常这个理由不成立
- 建议：理由换成「fixture 阶段出错一律计 error，所以用显式异常 + 说明性信息比裸 assert 更好读」，别把 error/failed 说成两者的区别

### `03b-api-automation#34` · 原文有问题（我们对） · 把握 high

**题**：11. 如何实现接口测试报告？

- 我们：主力：allure-pytest，步骤、附件、分类、趋势
- 原文：第一步安装插件：pytest-allure-adaptor
- 问题：原文过时：pytest-allure-adaptor 是 Allure 1 时代的插件，早已废弃，现行插件是 allure-pytest（我们写对了）
- 建议：保持现状，别照原文回填插件名；如要补充可加一句「老资料里的 pytest-allure-adaptor 已废弃」

### `03b-api-automation#4` · 我们写错了 · 把握 medium

**题**：1. Java接口自动化框架是如何搭建的？用了哪些技术栈？

- 我们：发请求：RestAssured（底层 HttpClient / OkHttp）
- 原文：RestAssured用来发起HTTP接口请求
- 问题：RestAssured 底层是 Apache HttpClient，不走 OkHttp；同一副牌的第 3 题（#6）自己也写的是「底层仍然是 HttpClient」，两处对不上
- 建议：括号改成「底层 Apache HttpClient」，OkHttp 另起一行作为可选的原生客户端列出

### `03c-web-automation#13` · 原文有问题（我们对） · 把握 medium

**题**：11. PageFactory和普通Page类有什么区别？

- 我们：PageFactory 只是代理延迟查找，等待粒度粗、不支持参数化定位器
- 原文：PageFactory 支持懒加载…性能更好
- 问题：懒加载这半句对，但「性能更好」不成立：代理字段每次访问仍走一次 findElement，还多一层反射代理，并不比普通 Page 类快
- 建议：不用改我们的答案，这属于原文的错误结论

### `03c-web-automation#17` · 我们写错了 · 把握 medium

**题**：15. 如何处理富文本编辑器？

- 我们：示例用 body.clear()，表格却说 clear() 对 contenteditable 通常无效
- 原文：（此点原文未涉及）
- 问题：同一张卡自相矛盾：iframe 型代码示例里用 clear() 清空富文本 body，下面表格又说 clear() 对可编辑区通常无效，读者会不知道该信哪句
- 建议：统一成「全选+删除或调编辑器 API 清空」，把示例里的 body.clear() 去掉或加注说明它只在 designMode 文档下可用

### `03c-web-automation#24` · 原文有问题（我们对） · 把握 medium

**题**：5. 如何获取App元素的定位信息？

- 我们：工具清单给的是 Appium Inspector / Layout Inspector / adb dump
- 原文：Android 端用 uiautomatorviewer，Android SDK 自带
- 问题：原文过时：uiautomatorviewer 随旧版 SDK tools 一起废弃、当前 SDK 不再提供（且只能跑在 JDK 8）；原文提的 Appium Desktop 也已被独立的 Appium Inspector 取代
- 建议：不用改我们的答案，我们列的正是现行替代工具

### `03c-web-automation#41` · 原文有问题（我们对） · 把握 medium

**题**：15. 如何实现失败截图和录屏？

- 我们：Selenium 没有内建录屏，靠带录制能力的浏览器容器
- 原文：execute_cdp_cmd('Page.startScreencast') 录屏
- 问题：原文那段 CDP 只是开启帧流推送，Selenium 收不到 screencast 事件也不会落盘，照抄拿不到任何视频文件；我们的说法是对的
- 建议：不用改我们的卡，保持「Selenium 无内建录屏，用容器录制或 Playwright trace」即可

### `03d-app-automation#24` · 原文有问题（我们对） · 把握 medium

**题**：13. 如何实现多设备并发测试？

- 我们：appium server -p 4723 --base-path /
- 原文：appium -p 4723 -bp 4724 -U ...
- 问题：-bp（bootstrap port）和 -U 都是 Appium 1 的服务端参数，Appium 2 已经移除，设备和端口改由 capabilities（udid / systemPort / wdaLocalPort）指定。我们的写法才是现行的。
- 建议：不用改；若要补充可注明"别照抄 -bp/-U，那是 Appium 1 的服务端写法"。

### `03d-app-automation#29` · 原文有问题（我们对） · 把握 medium

**题**：18. 如何获取App性能数据（CPU、内存、流量）？

- 我们：读 /proc/<pid>/net/dev 或按 App 的 uid 统计流量
- 原文：cat /proc/net/xt_qtaguid/stats
- 问题：xt_qtaguid 这个流量统计节点在 Android 9 之后已被移除（改走 eBPF），原文这条命令在新系统上取不到数据。我们没用它。
- 建议：不用改；可选在卡里加一句"老教程里的 xt_qtaguid 在 Android 9+ 已失效"。

### `04-performance-observability#123` · 原文有问题（我们对） · 把握 high

**题**：14. 如何优化大数据量分页查询？

- 我们：用 JOIN 派生表：子查询只在索引上取 20 个 id，再回表取完整行
- 原文：延迟关联示例写成 IN (SELECT id ... LIMIT 10000,10)
- 问题：原文那条示例 SQL 在 MySQL 上直接报错——MySQL 不支持在 IN/ALL/ANY 子查询里使用 LIMIT（ERROR 1235）；我们卡片用的 JOIN 派生表写法才是正确的延迟关联
- 建议：这条是原文的问题，我们不用改；若日后要引用原文示例，需注明 IN + LIMIT 在 MySQL 跑不通，必须改成 JOIN 派生表

### `04-performance-observability#35` · 原文有问题（我们对） · 把握 high

**题**：6. JMeter如何实现阶梯式加压？

- 我们：原生 Ramp-up 只有「爬」没有「稳」，阶梯要靠多线程组错峰或插件
- 原文：阶梯式加压主要通过线程组的Ramp-Up时间配置实现
- 问题：原文把 Ramp-Up 的线性平滑加压当成阶梯式加压，缺了阶梯的核心特征——每档要 hold 一段时间取稳态；我们的写法是对的
- 建议：不用改我们的答案，「爬一档→稳一段→再爬」这个定义保留

### `04-performance-observability#40` · 原文有问题（我们对） · 把握 high

**题**：11. JMeter如何实现动态参数，比如随机生成手机号？

- 我们：注册手机号这类服务端强校验唯一性的字段，用 __Random 一定会撞号，必须走 CSV
- 原文：用这种方式生成大量不重复的测试账号
- 问题：原文用 __Random 随机数当作「不重复」的注册账号来源，随机不保证唯一，压注册接口时会撞号、错误率随时间上升；我们卡里的判断是对的
- 建议：不用改我们的答案，保留「唯一性字段一律走 CSV 预生成」这条结论即可

### `04-performance-observability#42` · 我们写错了 · 把握 medium

**题**：13. JMeter如何实现接口加密？比如MD5签名？

- 我们：让开发把工具类打个 jar，放到 JMeter 的 lib/ext 下（依赖 jar 放 lib）
- 原文：（原文只讲 BeanShell，未涉及放包位置）
- 问题：位置说反了：JMeter 的约定是 lib 放普通工具/依赖 jar，lib/ext 只放 JMeter 自己的组件和插件；签名工具类属于前者。两处都在 classpath 上所以能跑通，但给出的规则是反的
- 建议：改成签名工具类和它的依赖统一放 lib，lib/ext 留给 JMeter 组件与插件

### `04-performance-observability#45` · 原文有问题（我们对） · 把握 high

**题**：16. JMeter中如何处理HTTPS请求？

- 我们：JMeter 内置了信任一切证书的 SSL 上下文，自签名/过期证书都不会挡住压测
- 原文：遇到证书验证问题，可以勾选"使用KeepAlive"
- 问题：原文把 KeepAlive 说成能解决证书验证问题，两者毫无关系（KeepAlive 只影响连接复用）；我们卡里对默认信任证书和 mTLS 的说明是对的
- 建议：不用改我们的答案；若要更稳，可在卡里补一句「KeepAlive 只管连接复用，与证书校验无关」作为反例

### `04-performance-observability#60` · 原文有问题（我们对） · 把握 high

**题**：11. 什么是容量规划？如何进行容量规划？

- 我们：机器数 = 目标峰值 TPS ÷ 单机拐点 TPS × 安全系数
- 原文：10万/3600≈28 TPS，考虑3倍冗余，至少需要4台服务器
- 问题：原文算例自相矛盾：需求 28 TPS、单机 800 TPS，就算乘 3 倍冗余也只要 1 台，推不出 4 台；而且用小时均值当峰值，漏掉了峰谷比
- 建议：不用改我们的答案，我们已经写了「峰值不是平均值」和机器数公式；可加一句「别拿日/时均值直接除，先按峰值时段折算」

### `06a-llm-engineering#22` · 漏了必答点 · 把握 high

**题**：大模型为什么会出现幻觉？怎么缓解？

- 我们：成因五条：训练目标、参数化存储无来源、知识边界、上下文不足、采样与对齐副作用
- 原文：根因一：训练数据本身就有错
- 问题：最基础的一条成因——预训练语料本身就含错误、互相矛盾、过时的信息——我们完全没列，读者可能以为数据质量与幻觉无关。
- 建议：在成因图里补一条「语料本身有噪声/错误/过时」，同时保留现有立场：即使数据全对，续写机制照样会幻觉。

### `06a-llm-engineering#26` · 原文有问题（我们对） · 把握 high

**题**：请讲一下 LoRA 技术，除了减少参数量，它还有哪些优点？

- 我们：B 初始化为零，保证训练起点与原模型完全一致
- 原文：A 和 B 是两个小矩阵（可训练，随机初始化）
- 问题：原文说 A、B 都随机初始化，那样起点 BA≠0，一开机就扰动了基座；正确做法是 A 高斯随机、B 置零——我们卡片写的是对的，是原文错。
- 建议：卡片不用改；后续若照原文补内容，注意别把「A、B 都随机初始化」抄进来。

### `06b-rag#15` · 漏了必答点 · 把握 medium

**题**：怎么量化你的 RAG 效果？

- 我们：忠实性/groundedness、答案相关性、上下文召回率、Precision@k
- 原文：生成层用 RAGAs 框架的四个核心指标
- 问题：指标概念我们全都覆盖了，但从头到尾没出现业界标准框架名 RAGAs 及其官方指标名（Faithfulness / Answer Relevancy / Context Recall / Context Precision），面试官通常就是听这几个关键词
- 建议：在生成层表格里给每个指标补上对应的英文官方名，并加一句「工程上常直接用 RAGAs 这类框架跑，本质是 LLM-as-judge」，保留我们已有的校准与成本提醒

### `06b-rag#23` · 漏了必答点 · 把握 medium

**题**：在 RAG 中 Embedding 究竟是什么？如何选择和评估一个 Embedding

- 我们：只给选型维度和评测流程，全篇未出现任何具体 embedding 模型名
- 原文：中文优先 BGE / bge-m3 / Qwen3-Embedding
- 问题：题目问「如何选择」，面试官接着必然追问「你用的哪个」；我们连一个候选模型名都没有，答不出落地选择
- 建议：在硬约束那一节后加一小段候选清单（中文开源、多语言、API 三类各举一两个代表），保持「以自测为准」的结论不变

### `06c-agent#13` · 漏了必答点 · 把握 medium

**题**：Agent 推理模式有哪些？ReAct 是啥？具体是怎么实现的？

- 我们：一览从「单跳调用」直接跳到 ReAct、Plan-and-Execute…
- 原文：最基础是直接回答，其次 CoT，再到 ReAct
- 问题：「推理模式有哪些」这一问漏了 CoT 这一档，而 ReAct 恰恰是在 CoT 上加 Action/Observation 演化来的，少这一档，后面「ReAct 解决了什么」的递进就没有起点
- 建议：在单跳调用和 ReAct 之间补一档 CoT（Zero-shot / Few-shot 两种触发、只能纯文字推理拿不到外部数据），并写明 ReAct = CoT + Action + Observation

### `06c-agent#16` · 漏了必答点 · 把握 medium

**题**：请你介绍一下 AI Agent 的记忆机制，并说明在实际开发中

- 我们：分「短期（工作）记忆」和「长期记忆（语义/情节/程序）」两层
- 原文：记忆机制分四层：感知记忆、短期、长期、实体记忆
- 问题：这题原文把「四层记忆」当答题骨架，其中「感知记忆」「实体记忆」两个名词我们一个都没出现。内容上我们其实覆盖了（当次输入、结构化事实都讲了），但如果面试官照这个提纲追问「除了长短期还有哪两种」，按我们的卡答不上名字。
- 建议：在分类那节加一句括号补充：把「当次调用的原始输入」叫感知记忆、把「抽取成结构化字段的稳定事实」叫实体记忆，说明是同一批东西的另一套叫法，不动我们现有的两层框架。

### `06c-agent#4` · 漏了必答点 · 把握 medium

**题**：Agent 记忆压缩通常有哪些方法？

- 我们：五类：截断、摘要、结构化抽取、外置检索、分层老化
- 原文：四种方法之一是重要性过滤，按价值不按时间
- 问题：「按重要性打分筛选」只在「去重与老化」里带了半句衰减，没有作为独立一档，而它和按时间截断是完全不同的维度（时间不等于重要性），面试里是四选一的必答项
- 建议：把重要性过滤单列一档：两种打分方式（关键词规则打分 / LLM 逐条打分，后者准但贵、适合批量清理），再补一句 Observation Masking——不删除，只在拼 prompt 时按当前阶段隐藏无关条目，信息损失可逆

### `06c-agent#8` · 漏了必答点 · 把握 medium

**题**：如何设计多 Agent 的协作与动态切换机制？

- 我们：先定拓扑，再讲动态切换三做法与交接契约、防失控
- 原文：协作靠两件事：消息传递和共享状态
- 问题：切换那半边答得很足，但「协作」那半边没有把两种通信方式拿出来对比：消息传递（走队列、发送方不需要知道接收方是谁、要额外中间件）vs 共享状态（同一个 State 对象直连、要分层与只追加防覆盖）。这是这题最常被追问的第一层
- 建议：在拓扑之前补一小节「通信方式选型」：依赖强、流水线型就用共享状态并规定只追加不覆盖；要各 Agent 互不感知、并行解耦就用消息传递

### `06d-tool-calling-mcp#10` · 漏了必答点 · 把握 medium

**题**：什么是 MCP（模型上下文协议）？讲讲它的核心内容？

- 我们：传输方式：本地进程用标准输入输出，远程服务走 HTTP 流式
- 原文：底层通信用 JSON-RPC 2.0
- 问题：「核心内容」清单里少了消息格式这一项：只列了传输方式，没提底层统一用 JSON-RPC 2.0，也没提它是 Anthropic 2024 年底发布的开放协议。这两点是这题被点名要求答到的基础事实。
- 建议：在「它标准化了什么」里补一条「消息格式：统一 JSON-RPC 2.0，与传输方式解耦」，一句话里补上「Anthropic 2024 年底提出的开放协议，不是 Claude 专属」。

### `06d-tool-calling-mcp#2` · 漏了必答点 · 把握 medium

**题**：什么是 A2A 协议？它和 MCP 协议的区别是什么？

- 我们：能力自述（业界常用「能力卡片」这类概念）
- 原文：Google 发布；核心是 Agent Card 与 Task
- 问题：「什么是 A2A」这半问没落到实处：没说是 Google 发起的开放协议，也没点名 Agent Card（含 Skill 列表、well-known 路径发布）和 Task 生命周期这两个核心对象，只用「能力卡片这类概念」带过。
- 建议：在一句话或对比表里补上「Google 发起的开放协议」「能力自述 = Agent Card」「任务对象 = Task，有 submitted/working/completed/failed 生命周期」，名词点到即可，后面照旧提醒字段会演进。

### `06d-tool-calling-mcp#6` · 漏了必答点 · 把握 medium

**题**：有没有用过大模型的网关框架？网关层解决了什么问题？

- 我们：不必纠结具体产品名
- 原文：我用过 LiteLLM，最活跃的开源 LLM 网关
- 问题：题目前半句直接问「有没有用过哪个网关框架」，我们主动劝人别提产品名，整张卡一个可举例的框架都没有，答的时候会显得没落地。职责和坑讲得很全，但缺一句「市面上有哪些」。
- 建议：在选型或结尾处加一行框架清单（LiteLLM、One API、PortKey、Kong AI Gateway、自建 Nginx/Envoy 一类），说明各自定位，再接原来那句「面试官真正想确认的是你知道这一层该承担什么」。

### `06d-tool-calling-mcp#9` · 漏了必答点 · 把握 medium

**题**：大模型的 Function Call 能力是怎么训练出来的？

- 我们：用可验证的奖励…而不是人工打分
- 原文：人类排序训奖励模型，再用 PPO 优化
- 问题：SFT 那一段没问题，但第二阶段我们只讲了可执行验证式奖励，并明确写「而不是人工打分」，等于把主流公开做法（RLHF：人类偏好排序 → 奖励模型 → PPO，以及低成本替代 RLAIF）排除在外。被追问「RLHF 在这里起什么作用」会答不上。
- 建议：把「而不是人工打分」改成「除了人类偏好那套（RLHF/RLAIF：偏好排序训奖励模型再做强化），工具调用还能用可验证奖励——对错客观可判，这是它相比通用对齐的优势」，两条并列而不是替代。

### `06e-langchain#12` · 漏了必答点 · 把握 medium

**题**：LangChain 大版本升级有哪些核心变化？

- 我们：六条脉络：可组合原语、包拆分、原生工具调用+图、记忆抽象、可观测、schema 驱动
- 原文：v1 以 create_agent 为高层入口，middleware 承接横切能力，旧 Chain 移到 langchain-classic
- 问题：原文四条主线里最新的那条（v1 聚焦 Agent：高层入口 + middleware 扩展 + 旧能力迁到 classic 包而非删除）我们一条都没提，读者会以为旧 Chain 是被直接删掉的
- 建议：在「几条主要脉络」里补一条：最新大版本把主线收敛到 Agent，提供统一的高层创建入口，动态提示词/工具筛选/摘要/重试/人工介入这类横切能力改用中间件插入，旧的 Chain 等能力迁到独立的 classic 包保持可用、可渐进迁移

### `10-golang#104` · 我们写错了 · 把握 high

**题**：7.5 接口之间可以相互比较吗？

- 我们：err == io.EOF：io.EOF 被隐式装箱成接口再比
- 原文：接口值与非接口值比较时，先把非接口值转成接口值
- 问题：io.EOF 的静态类型本来就是 error 接口，这段其实是接口对接口比较，不存在装箱，举的例子撑不起「接口和非接口比较」这个小标题
- 建议：换一个真正的非接口例子，比如具体类型的错误值直接和 error 变量比，再说明非接口一侧会被隐式转成接口

### `10-golang#118` · 我们写错了 · 把握 medium

**题**：9.10 g0栈和用户栈是如何进行切换的？

- 我们：Go 的调用约定让其余寄存器由被调用者保存
- 原文：（原文未涉及这一点）
- 问题：Go 的 ABI 里没有 callee-save 寄存器；切换只需存 SP/PC/BP，是因为切换点在调用边界上、活跃值已经由调用方溢出到栈。卡片后文自己写的是「调用者保存的寄存器早已入栈」，前后自相矛盾
- 建议：把表格那句改成「调用点上的活跃值已由调用方溢出到栈，Go ABI 没有被调用者保存的寄存器」，和后文统一

### `10-golang#122` · 我们写错了 · 把握 medium

**题**：10.4 Channel是分配在栈上，还是堆上？

- 我们：只有极少数创建后完全不外传的情况才可能留在栈上
- 原文：channel 分配在堆上
- 问题：编译器没有 chan 的栈分配路径，make(chan) 一律走 runtime.makechan → mallocgc；即使 -m 打印 does not escape，hchan 本体仍然在堆上，只有那个 *hchan 变量在栈上
- 建议：把「可能留在栈上」改成「hchan 始终堆分配，-m 显示 does not escape 也不例外」，保留「chan 变量本身是栈上的指针」这句

### `10-golang#137` · 我们写错了 · 把握 medium

**题**：11.13 Go 的 GC 如何调优？

- 我们：容器里 GOMAXPROCS 默认取的是宿主机的核数，而不是 CPU limit
- 原文：（原文只提 GOGC，未提 GOMAXPROCS）
- 问题：较新的 Go 运行时在 Linux 上已经会参考 cgroup 的 CPU 配额来决定默认 GOMAXPROCS，这句「默认一定取宿主机核数」对新版本不再成立；卡里虽然带了「随版本演进」的免责句，但主句仍是断言
- 建议：把主句改成「老版本 Go 在容器里默认按宿主机核数设 GOMAXPROCS；较新版本已会参考 cgroup CPU 配额。上线前确认自己的 Go 版本，必要时显式设置或用 automaxprocs」

### `10-golang#138` · 原文有问题（我们对） · 把握 high

**题**：11.14 如何观察 Go GC？

- 我们：行首的 3% 是程序启动至今 GC 累计占用的 CPU 百分比
- 原文：字段表写「2% = 该 GC 周期中 CPU 的使用率」
- 问题：gctrace 行首那个百分比是自程序启动以来 GC 累计占用 CPU 的比例，不是单个 GC 周期的占用率；我们写对了，是原文的字段解释有误
- 建议：我们的卡不用改；如果以后要照抄原文的字段对照表，记得把这一项改成「自程序启动累计的 GC CPU 占比」

### `10-golang#139` · 我们写错了 · 把握 high

**题**：12.1 开启100个协程，顺序打印1-1000

- 我们：易错点表：用有缓冲 channel → 令牌可能堆积，交接不再是同步点 → 一律用无缓冲
- 原文：（原文只给代码，未讨论缓冲）
- 问题：全系统只有一个令牌在环上传，永远不会「堆积」；100 个 channel 各自加缓冲也不影响打印顺序。「一律用无缓冲」这种绝对化结论和真实机制不符
- 建议：把这一行从「错误写法」改成「无缓冲更能表达同步语义，但本题顺序由令牌唯一保证，缓冲不会破坏顺序」

### `10-golang#145` · 我们写错了 · 把握 medium

**题**：12.7 使用go实现1000个并发控制并设置执行超时时间1秒

- 我们：time.After 每次都新建一个 timer，且在到期前不会被提前回收
- 原文：（原文只给代码，未提 time.After）
- 问题：「到期前不会被回收」是旧版本的行为；较新的 Go 已经让不再被引用的 timer/ticker 即使没 Stop 也能立刻被 GC 回收，这句在现在的版本上是错的（建议用 NewTimer+Stop 的结论本身仍成立）
- 建议：把这条改成「老版本 Go 里 time.After 的 timer 到期前不会被回收，新版本已可提前回收；但高频循环里仍推荐 NewTimer + defer Stop，语义更可控」

### `10-golang#44` · 我们写错了 · 把握 high

**题**：1.11 go 打印时 %v %+v %#v 的区别？

- 我们：要先转成别名类型：type alias Point; Sprintf("%v", alias(p))
- 原文：原文未涉及该点
- 问题：示例代码写的是「定义一个新类型」，靠丢掉方法集才不递归；而真正的类型别名 type alias = Point 会保留 String() 方法，照样无限递归。叫它「别名类型」会把人带沟里
- 建议：把「别名类型」这个叫法改成「基于原类型定义一个新的 defined type」，并补一句「不能写成 type alias = Point，那是别名，方法集还在」

### `10-golang#45` · 我们写错了 · 把握 high

**题**：1.12 Go语言中空 struct{} 占用空间么？

- 我们：零大小字段在末尾时，编译器会额外补齐一个字宽
- 原文：原文未涉及该点
- 问题：编译器补的是 1 字节，再按结构体对齐向上取整；例子里 int64 版本变 16 是对齐到 8 的结果，不是固定补一个字宽（struct{x int8; y struct{}} 只有 2 字节）
- 建议：把「补一个字宽」改成「补 1 字节，再按对齐规则向上取整」，保留 int64 例子说明为什么这里恰好多出 8 字节

### `10-golang#46` · 我们写错了 · 把握 high

**题**：1.13 Go语言中，空 struct{} 有什么用？

- 我们：它是最后一个字段时，编译器会补一个字宽的填充
- 原文：原文未涉及该点
- 问题：同上题：实际是补 1 字节再按对齐向上取整，「一个字宽」只在字长对齐的结构体里碰巧成立
- 建议：与 1.12 那题统一口径，改成「补 1 字节 + 按对齐取整」

### `10-golang#52` · 原文有问题（我们对） · 把握 high

**题**：1.19 Go语言的多返回值是如何实现的？

- 我们：较新版本（amd64 自 1.17 regabi）参数与返回值优先走寄存器
- 原文：多返回值靠调用方栈帧预留空间实现
- 问题：原文只描述了 Go 1.17 之前的纯栈 ABI，已过时；我们补的「两代实现（栈 → 寄存器 ABI）」才是当前事实
- 建议：不要按原文回改；保留我们的两代 ABI 说法即可，必要时注明原文停留在旧 ABI

### `10-golang#77` · 原文有问题（我们对） · 把握 high

**题**：4.7 关闭Channel会产生异常吗？

- 我们：三种 panic：关闭 nil、重复关闭、关闭后发送
- 原文：关闭只有接收方向的 channel 将导致 panic
- 问题：关闭只接收方向的 channel（<-chan T）是编译错误，不是运行时 panic；原文把它和三种 panic 并列了
- 建议：别照原文补第四种 panic；要补就写成「关闭只接收 channel 在编译期就报错，压根跑不到运行时」

### `10-golang#78` · 原文有问题（我们对） · 把握 high

**题**：4.8 往一个关闭的Channel写入数据会发生什么？

- 我们：chansend 一进来就先检查 closed 标志，和缓冲区空位无关
- 原文：这个检查甚至在获取 mutex 锁之前就进行
- 问题：runtime 里 send on closed channel 的 panic 发生在 lock(&c.lock) 之后；加锁前那次 c.closed 判断只服务于非阻塞发送的快速返回，不会 panic
- 建议：保持我们现在的表述，不要按原文补「加锁前就 panic」；想更严谨可写成「拿到 hchan 锁后第一件事就是查 closed」

### `10-golang#79` · 我们写错了 · 把握 medium

**题**：4.9 什么是select？

- 我们：time.After 每轮新建 Timer，超时前不会被回收，高频循环下会堆积
- 原文：原文未涉及该点
- 问题：Go 1.23 起，不再被引用的 Timer/Ticker 立即可被 GC，不用等触发；「循环里 time.After 会堆积到超时」这个经典说法对 1.23+ 已不成立（每轮仍有一次分配开销）
- 建议：给这条加版本界线：Go 1.23 之前确实会堆到超时才回收，1.23 起可提前回收；推荐 NewTimer/Reset 或 context 的理由改成「省掉每轮分配、超时可控」

### `10-golang#92` · 原文有问题（我们对） · 把握 high

**题**：5.11 read map和dirty map之间有什么关联？

- 我们：两者同名 key 共享同一个 *entry；expunged 的 entry 不在 dirty 里
- 原文：value 要么是最终值，要么是指向 dirty 条目的特殊指针
- 问题：原文把 read 里的 value 说成「指向 dirty 对应条目的特殊指针」，并称 read 的数据在 dirty 里一定存在；实际是两边持有同一个 *entry 指针，nil/expunged 只是删除标记，而 expunged 的 entry 恰恰不在 dirty 里
- 建议：我们的写法是对的，不要往原文那边靠；如果想留痕，可在卡里加一句「原文此处描述不准确」

### `11-data-structures-algorithms#26` · 我们写错了 · 把握 high

**题**：平衡二叉树结构是怎么样的？

- 我们：mermaid「平衡」子图里根节点标 30 BF=0，左孩子 20 BF=0，右孩子 40 BF=-1（40 还带一个孩子 50）
- 原文：平衡因子＝左右子树高度差，绝对值不超过 1
- 问题：按我们自己给的 BF 定义算，30 的左子树高 1、右子树高 2，BF 应该是 -1 而不是 0；这是一张专门用来演示 BF 的图，数字标错会误导人
- 建议：把根节点的标注从 BF=0 改成 BF=-1（树仍然是平衡的，结论不受影响），或者给 20 补一个孩子让 BF=0 真正成立

### `11-data-structures-algorithms#51` · 我们写错了 · 把握 high

**题**：二叉树有几种遍历方式？分别怎么实现？

- 我们：Morris 遍历：利用叶子节点的空右指针建立「线索」指回前驱
- 原文：原文未提 Morris 遍历
- 问题：线索的方向说反了：Morris 中序是把「左子树最右节点（中序前驱）」的空右指针指向当前节点，也就是指向后继；而且那个节点未必是叶子，只是右指针为空
- 建议：改成「把左子树中最右节点（中序前驱）的空右指针指向当前节点（它的中序后继），遍历回来时再断开复原」

### `14-operating-system#38` · 我们写错了 · 把握 medium

**题**：线程切换详细过程是怎么样的？上下文保存在哪里？

- 我们：浮点/向量寄存器「常见做法是懒切换」，标记不可用、真用到才触发异常保存加载
- 原文：（原文只讲到上下文保存在 TCB，未涉及此点）
- 问题：懒切换（lazy FPU restore）在现代 Linux 上已经不是常见做法了——它因为 Lazy FP State Restore 这类侧信道问题被移除，内核改成了每次切换就直接存取，靠 XSAVE 系列指令的 init/modified 优化来省开销，效果类似但机制完全不同
- 建议：把「常见做法是懒切换」改成「早期用懒切换，出于安全问题现代内核已改为切换时直接保存恢复，靠 XSAVE 的 init/modified 优化避免搬运没用到的状态」，保留「不用浮点的线程几乎不付这份开销」这个结论

### `15-computer-network#45` · 原文有问题（我们对） · 把握 high

**题**：http 断点重传是什么？

- 我们：Content-Range: bytes 1024-2047/5000
- 原文：Content-Range:bytes 512000-/1024000
- 问题：原文示例缺了结束位置，Content-Range 语法必须是「起-止/总长」三段齐全，我们写的格式才对
- 建议：我们不用改；只在心里记住原文这个示例是笔误，别照它把结束位置删掉

### `15-computer-network#47` · 原文有问题（我们对） · 把握 high

**题**：HTTPS是如何防范中间人的攻击？

- 我们：非对称加密只用在握手阶段协商出对称密钥
- 原文：客户端用证书公钥加密通信数据，服务端私钥解密
- 问题：原文讲的是已被淘汰的 RSA 密钥传输模型；现代 TLS 用 ECDHE 协商对称密钥、证书私钥只负责签名，我们的写法是对的
- 建议：我们不用改；可在加分点补一句「TLS 1.3 已移除 RSA 密钥交换，证书私钥只做签名」，顺手把旧模型纠正掉

### `15-computer-network#58` · 原文有问题（我们对） · 把握 high

**题**：cookie和session有什么区别？

- 我们：Session 生命周期由服务端的过期时间决定，通常几十分钟
- 原文：默认情况下，当用户关闭浏览器时，Session 结束
- 问题：原文把「关浏览器 Session 就结束」当成 Session 自身的生命周期；实际关浏览器只是删掉了那条会话 Cookie，服务端 Session 对象要等超时才回收
- 建议：不按原文改；在生命周期那行补半句「关浏览器只是丢了 Session ID，服务端数据要等超时才清」，直接点破这个常见误解

### `15-computer-network#60` · 我们写错了 · 把握 medium

**题**：如果客户端禁用了cookie，session还能用吗？

- 我们：localStorage / sessionStorage 不受「禁用 Cookie」开关的影响，是两套独立机制
- 原文：（原文未提 Web Storage）
- 问题：规范上确实是两套机制，但主流浏览器把它们绑在同一个站点数据开关下：Chrome/Firefox 选「阻止所有 Cookie」后读 localStorage 会抛 SecurityError，先说「不受影响」容易让人记成「禁 Cookie 还能拿 localStorage 存 Session ID」
- 建议：把主句和例外调个头：先说「阻止所有站点数据一般会连 Web Storage 一起禁掉，访问会抛异常」，再补「只屏蔽第三方 Cookie 时不受影响」

### `15-computer-network#68` · 原文有问题（我们对） · 把握 high

**题**：前端是如何存储JWT的？

- 我们：sessionStorage 刷新页面后保留，只是不跨标签页共享
- 原文：刷新页面或在新标签页打开需要重新认证
- 问题：原文说 sessionStorage 刷新就要重新认证，实际它只在标签页关闭时才清除，刷新不丢；我们表格写的「刷新后保留」才对
- 建议：我们不用动；以后回头对照原文时记住这句是原文的错，别据此改掉「刷新后保留」

### `15-computer-network#71` · 原文有问题（我们对） · 把握 high

**题**：Nginx有哪些负载均衡算法？

- 我们：按响应时间分配属商业版/第三方模块；least_conn 是内置
- 原文：Nginx支持的负载均衡算法包括……最短响应时间
- 问题：原文把「最短响应时间」列成 Nginx 内置算法，实际 least_time 只在 NGINX Plus，开源侧只有第三方 fair 模块；原文还漏了真正内置的 least_conn
- 建议：保持现在的划分（least_conn 内置、响应时间类归商业版/第三方），不要按原文补一条「内置最短响应时间」

### `15-computer-network#77` · 漏了必答点 · 把握 medium

**题**：客户端发送的第一个 SYN 报文，服务器没有收到

- 我们：重传次数由系统参数控制，各系统默认值不同，不必背具体秒数
- 原文：tcp_syn_retries 默认 5；1+2+4+8+16+32=63 秒
- 问题：这题面试官几乎必追问「重传几次、总共等多久」，卡里刻意不给参数名和量级，真被问到接不上；顺带一提原文的「默认 5」是旧值，现代 Linux 内核 tcp_syn_retries 默认是 6
- 建议：补一句：Linux 由 tcp_syn_retries 控制（老内核 5、现代内核 6），退避是 1/2/4/8/16/32 秒，放弃前总计约 63~127 秒

### `15-computer-network#98` · 原文有问题（我们对） · 把握 high

**题**：CSRF攻击是什么？

- 我们：攻击者从头到尾读不到你的 Cookie，也读不到响应内容，只是触发了一个请求
- 原文：诱导用户执行恶意操作，从而获取用户数据；设置 CSP 来防止跨站请求
- 问题：原文这条有两处不准：CSRF 方读不到响应，谈不上「获取用户数据」；CSP 是 XSS 的防线而非 CSRF 的主力手段（只有 form-action 沾一点边），原文却把它列成 CSRF 解法。我们的写法是对的，问题在原文。
- 建议：这张卡不要按原文回填。可以顺手加一句「CSP 主要防 XSS，别当成 CSRF 的防御手段」，把这个常见误解直接标死。

### `16-database#36` · 我们写错了 · 把握 medium

**题**：CHAR 和 VARCHAR有什么区别？

- 我们：VARCHAR(50) 和 VARCHAR(200) 存同样的内容，磁盘占用一样吗？一样。
- 原文：（原文未涉及此追问）
- 问题：和同卡片上文自相矛盾：长度前缀是 1 还是 2 字节取决于该列最大字节数是否超 255，utf8mb4 下 VARCHAR(50) 是 1 字节前缀、VARCHAR(200) 是 2 字节，磁盘其实差 1 字节
- 建议：把「一样」改成「几乎一样，只差长度前缀那 1 字节」，结论仍落在真正的差别是内存和排序开销上

### `16-database#41` · 原文有问题（我们对） · 把握 high

**题**：MySQL的关键字in和exist

- 我们：现代优化器会把 IN 子查询改写成 semi-join，别背「EXISTS 更快」的老规则，以 EXPLAIN 为准
- 原文：EXISTS 的性能优于 IN，特别是当子查询的表很大时
- 问题：原文沿用「EXISTS 一定比 IN 快」的旧经验，MySQL 5.6 起 IN 子查询有 semi-join / 物化 / FirstMatch 等策略，这条已不普遍成立；我们的说法是对的
- 建议：不要按原文把结论改回「EXISTS 更快」，可在卡片里补一句点明原文这条是过时经验

### `16-database#84` · 原文有问题（我们对） · 把握 high

**题**：事务的隔离级别有哪些？

- 我们：RR 是事务内第一次快照读时建一次 ReadView 并复用
- 原文：可重复读是「启动事务时」生成一个 Read View
- 问题：原文说 RR 在 BEGIN 时就生成 ReadView，实际是第一次快照读时才生成（除非用 START TRANSACTION WITH CONSISTENT SNAPSHOT）；原文自己在「A事务提交的数据B能否看见」那题里的说法反而和我们一致
- 建议：保持我们的表述，不要改成「启动事务时」；两张卡的口径已经统一，不必动

### `17-redis-cache#30` · 我们写错了 · 把握 high

**题**：Redis 中 set和zset区别是什么？

- 我们：随机弹出：Set 有 SPOP/SRANDMEMBER，ZSet「没有等价的原生随机弹出」
- 原文：（原文未涉及该对比项）
- 问题：ZSet 自 6.2 起有 ZRANDMEMBER，正是 SRANDMEMBER 的对应命令；另有 ZPOPMIN/ZPOPMAX，说「没有等价」不准确
- 建议：把这一行改成：Set 用 SPOP/SRANDMEMBER，ZSet 有 ZRANDMEMBER（随机取）和 ZPOPMIN/ZPOPMAX（按分数弹），只是没有「随机弹出并删除」这一条

### `17-redis-cache#63` · 我们写错了 · 把握 medium

**题**：Redis大key如何解决？

- 我们：Hash 分桶：user:profile:{id} → user:profile:{id}:{hash(field) % N}
- 原文：拆分大 Key 能对数据分片间的内存平衡起到显著作用
- 问题：同一张牌组里 {} 被定义为 Cluster 的 hash tag；照这个写法所有分桶都带同一个 {id}，会被强制落到同一个 slot、同一个节点，恰好达不到「分片间内存均衡」这个目的
- 建议：把示例里的花括号换成不会被误读为 hash tag 的占位写法（如 user:profile:<id>:<bucket>），并补一句：Cluster 下分桶 key 不要带同一个 hash tag，否则拆了也还在一个节点上

### `18-linux-troubleshooting#44` · 我们写错了 · 把握 medium

**题**：怎么判断操作系统有没有在内存替换？

- 我们：swappiness 0~100，越大越倾向于把匿名页换出去，默认常见 60
- 原文：（原文未涉及 swappiness，这是我们自己补的）
- 问题：取值上限写成 100 已过时：较新内核（5.8 起）把 vm.swappiness 的最大值提到了 200，用于在有高速交换设备时更激进地换出匿名页
- 建议：把范围改成「传统 0~100，新内核上限放宽到 200」，默认值 60 保持不变

### `20a-system-design#9` · 我们写错了 · 把握 high

**题**：如何设计一个秒杀场景？

- 我们：Lua：if tonumber(redis.call('get',KEYS[1])) >= tonumber(ARGV[1]) then decrby
- 原文：（原文只说用 Lua 脚本做原子扣减，没给代码）
- 问题：这段 Lua 在库存 key 不存在时会直接报错而不是返回 -1：GET 缺失键返回 false，tonumber(false) 得到 nil，nil 和数字比较会抛运行时错误，脚本整体失败；另外 DECRBY 的返回值可能正好是 0 或负数，和 -1 这个失败哨兵值语义容易混
- 建议：先把库存读进局部变量并判空（local s = tonumber(redis.call('get',KEYS[1])); if not s then return -1 end），再做比较扣减；成功/失败用两个不会歧义的返回值区分

### `20b-distributed#8` · 我们写错了 · 把握 medium

**题**：让你设计一个分布式 ID 发号器，怎么设计？

- 我们：号段模式优点：ID 短、纯递增、无时钟依赖
- 原文：号段模式是「批发」号段到本地内存分配
- 问题：同一张卡里号段服务是多实例部署的，各实例各持一段，并发发号时全局看只是趋势递增、会来回跳，不是「纯递增」；这和我们在 20b#15 强调的「趋势递增 ≠ 连续递增」自相矛盾
- 建议：把「纯递增」改成「单实例内严格递增，多实例下是趋势递增（会跳号、不连续）」

### `20c-message-queue#21` · 我们写错了 · 把握 medium

**题**：RocketMQ延时消息的底层原理

- 我们：延迟消息的 msgId 和原消息不同，因为到期时是重新写入的一条新消息
- 原文：原文未提及此点
- 问题：到期重投时消息属性是整体复制过去的，UNIQ_KEY（也就是平时说的 msgId、控制台按 key 查的那个）保持不变，变的是 offsetMsgId。说「msgId 不同」会让人以为没法用 msgId 追踪一条延迟消息。
- 建议：改成「offsetMsgId 会变（物理位置变了），msgId/UNIQ_KEY 不变，所以控制台按 key 仍能追到同一条」，和 #24 的 ID 说法统一。

### `20c-message-queue#25` · 我们写错了 · 把握 high

**题**：RocketMQ消息积压了，怎么办？

- 我们：顺序消费模式下同一队列只能单线程串行，加线程数完全无效，只能加队列数
- 原文：原文未提及顺序消费这一条
- 问题：顺序消费的串行边界是「同一个队列」，锁是按 MessageQueue 加的。一个消费者实例若分到多个队列，线程池里不同线程可以并行消费不同队列，这时加消费线程数是有效的；无效的只是同一队列内部。
- 建议：把范围限定住：改成「同一队列内加线程无效；实例持有多个队列时加消费线程仍有效，真正的天花板还是队列数」。

### `20c-message-queue#36` · 我们写错了 · 把握 medium

**题**：RabbitMQ 和 AMQP 是什么关系？

- 我们：AMQP 1.0 RabbitMQ 通过插件支持；插件支持 MQTT、STOMP、AMQP 1.0
- 原文：原文未区分 0-9-1 与 1.0
- 问题：RabbitMQ 4.0 起 AMQP 1.0 已并入核心、默认启用，不再依赖 rabbitmq_amqp1_0 插件；MQTT / STOMP 才仍然是插件。卡片按旧版本口径写成「AMQP 1.0 靠插件」。
- 建议：把 AMQP 1.0 那一行改成「新版本已内建为核心协议、默认可用，旧版本才需要插件」，MQTT/STOMP 保留插件说法。（#39 里「通过插件支持 MQTT、STOMP、AMQP 1.0」同一处措辞可一起改。）

### `20c-message-queue#40` · 原文有问题（我们对） · 把握 high

**题**：RabbitMQ的底层架构是什么？

- 我们：要多副本必须用仲裁队列；镜像队列在网络分区下有一致性问题、已不推荐
- 原文：通过镜像队列，可以在多个节点上复制同一队列的内容
- 问题：原文仍把镜像队列当成 RabbitMQ 的高可用手段，但它已被官方弃用并在 4.0 中移除，当下的答案是仲裁队列。我们的卡片是对的，问题在原文。
- 建议：卡片不用改；只是后续如果拿原文回填内容，注意别把「镜像队列做高可用」写回来。

### `20c-message-queue#8` · 原文有问题（我们对） · 把握 medium

**题**：消息队列怎么选型？

- 我们：RabbitMQ 单 topic/队列数量多时内存压力大（未给数量级）
- 原文：甚至百万级的RabbitMQ（指支持主题数）
- 问题：原文正文说 RabbitMQ 主题可到百万级，既跟它自己表格里写的「万级」自相矛盾，也不符合实际——经典队列一个队列就是一个 Erlang 进程，队列上万后内存与管理开销已经很明显。我们的卡片没跟这个数字，是对的。
- 建议：卡片不用改；后续若要补主题数量级，按「RabbitMQ 万级、队列多时内存/管理开销显著」写，别采用原文的「百万级」。
